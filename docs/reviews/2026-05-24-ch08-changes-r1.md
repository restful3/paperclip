# ch08 Round 1 — Changes Applied

_2026-05-24, codex-b / Round 1 → 2 송부용_

## docs/report/08-usage.md — 8곳 수정

핵심: cross-chapter 정합 (approve_ceo_strategy SPEC 목표 / 현재 구현 흐름) + 코드 라인 정합.

```diff
- pnpm paperclipai run --bind lan   # authenticated/private + LAN 바인드
+ pnpm paperclipai run --bind lan   # 첫 실행(설정 없음) 때 authenticated/private + LAN 바인드

- PATCH /companies/:companyId/budgets ... companies.ts:140-149
+ PATCH /api/companies/:companyId/budgets ... companies.ts:117-150

- 생성 화면은 위 항목만 받는다(NewAgent.tsx:66-71, new-agent-hire-payload.ts:23-39).
+ 생성 화면의 핵심 identity/payload 항목은 위와 같지만, AgentConfigForm 안에서 default environment, adapter-specific config, run policy도 함께 받는다(AgentConfigForm.tsx:783-1193, new-agent-hire-payload.ts:23-39).

- 그 후 CEO가 자기 보고 라인을 *제안*한다 — CTO/CMO/CFO 등의 에이전트 정의가 approvals 큐로 다시 들어오고...
+ 후속 임원/에이전트도 현재 구현에서는 명시적인 agent hire 흐름으로 생성된다. require_board_approval_for_new_agents=true 이면 각 hire 가 hire_agent approval 로 들어가고, 보드 승인 후 활성화된다.

(4단계 흐름 — CEO 첫 heartbeat 전략 분해 → 현재 구현 흐름으로 재서술, approve_ceo_strategy = SPEC 목표 명시)

- `Dockerfile:76`
+ `Dockerfile:79-80`

- board-claim.ts:43-66
+ board-claim.ts:44-73, routes/access.ts:2454-2495

- /dashboard/live 는 회사 WebSocket 위에 활성/최근 에이전트 run 을 실시간으로 보여 준다 (DashboardLive.tsx:42-46).
+ /dashboard/live 는 /api/companies/:companyId/live-runs 조회와 run log polling 으로 활성/최근 에이전트 run 을 보여 준다 (ActiveAgentsPanel.tsx:78-81). WebSocket transcript streaming 은 useLiveRunTranscripts 의 realtime 옵션이 켜진 화면에서만 사용된다.
```

## 누적 LOC delta (그룹 B)

- ch06: ~30 / ch07: ~24
- ch08 R1: ~25 (F5 4단계 변경 포함)
- **그룹 B 누적: ~79 LOC** (LOC_CHECKPOINT 200 까지 121 LOC 여유)
