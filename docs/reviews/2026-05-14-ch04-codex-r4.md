# 챕터 04 리뷰 — 라운드 4

## 종합 판정

R3 ACCEPT 2건은 반영됐다. `getConfigSchema` 설명은 `acpx-local` + `cursor-cloud`로 정정됐고, plugin-managed skill 설명도 capability / manifest `skills` 배열 / SDK API 인용을 분리했다. 다만 타입 필드 line-range 설명과 Hermes `detectModel` 근거 범위에 아직 작은 사실 오류가 남아 있다.

## ACCEPT-worthy

### R4-A1. 위치: `04-adapters-and-skills.md:13`

- 현재: `supportsLocalAgentJwt`, `supportsInstructionsBundle` 등을 capability flags로 묶고 "`:407-431`"에 있다고 설명.
- 문제: `supportsInstructionsBundle`은 `:413`에 있지만, `supportsLocalAgentJwt`는 `:357`에 있다. 따라서 `supportsLocalAgentJwt`까지 `:407-431` capability flags 범위에 둔 설명은 실제 타입 정의와 맞지 않는다.
- 제안: `supportsLocalAgentJwt`는 앞쪽 optional field 범위(`:349-396`)에 넣고, capability flags 예시는 `supportsInstructionsBundle`, `instructionsPathKey`, `requiresMaterializedRuntimeSkills`, `getRuntimeCommandSpec`로 정리한다.
- 근거: `packages/adapter-utils/src/types.ts:349-357`, `packages/adapter-utils/src/types.ts:407-431`.

### R4-A2. 위치: `04-adapters-and-skills.md:19`

- 현재: `detectModel` 설명의 근거가 `server/src/adapters/registry.ts:106~117, 380~439`로 붙어 있다.
- 문제: Hermes `detectModel` import는 `:122`, adapter field 등록은 `:465`다. 현재 범위는 import와 등록 위치를 모두 정확히 포함하지 않는다.
- 제안: 근거를 `server/src/adapters/registry.ts:116-123, 456-465`로 고친다.
- 근거: `server/src/adapters/registry.ts:116-123`, `server/src/adapters/registry.ts:456-465`.

## DEFER

- 그림 7 SVG 내부 `cursor_cloud` 행 추가/재배치는 기존 OUT-OF-SCOPE 유지.
- 외부 인용 그림 번호 `4-1~4` 통합은 글로벌 번호 정합화 단계에서 처리.

## OUT-OF-SCOPE

- PDF 재빌드.

## 라운드 종결 신호

ACCEPT 2건이 있어 최종 승인 아님.
