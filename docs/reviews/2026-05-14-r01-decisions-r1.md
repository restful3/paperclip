# R01 라운드 1 결정

- ACCEPT 5 / REJECT 0 / DEFER 0

## ACCEPT 5건 (전부 substantive 사실/인용 위반)

### 1. L21 Anthropic workflow/agent quote — 원문과 인용 형식 불일치
- 원문 (https://www.anthropic.com/research/building-effective-agents):
  - "Workflows: systems where LLMs and tools are orchestrated through predefined code paths."
  - "Agents: systems where LLMs dynamically direct their own processes and tool usage, maintaining control over how they accomplish tasks."
- R01 본문은 (a) "Workflows:" 뒤 "systems where" 누락, (b) Agents 끝 "maintaining control over..." 누락.
- 결정: paraphrase 로 변환 (직접 quote는 truncation 위험 크므로 회피).

### 2. L25 Temporal Event History quote — 원문 truncation
- 원문 (https://temporal.io/blog/...): "When Temporal executes a Workflow, it records a full Event History — every single time code in the Workflow is run, every single time an Activity is called or returned, and more."
- R01은 "When Temporal executes a Workflow, it" 누락 + "and more" 누락.
- 결정: 원문 그대로 인용 (한 줄짜리 직접 인용은 정확하게 유지).

### 3. L33 LangGraph quote — 원문에 없음
- WebFetch 결과: "agent never loses progress across restarts" 는 LangGraph durable-execution 문서에 없는 표현.
- 실제 표현: "pause and later resume exactly where it left off"; "By preserving completed work, durable execution enables a process to resume without reprocessing previous steps."
- 결정: paraphrase 로 정정.

### 4. L38 n8n quote 및 사실 오류 (2중 오류)
- (a) "AI Agent node operates as a single LLM call" — 원문은 "an autonomous reasoning step that selects tools, loops until complete, and outputs a result" (multi-step loop).
- (b) "cannot orchestrate agents running on external infrastructure" — 원문은 "you can call LangChain, CrewAI, or AutoGen agents via HTTP requests from n8n workflows" (정반대).
- 같은 표(L38)의 "핵심 패턴" 컬럼에도 "AI Agent 노드는 단일 LLM 호출" 잔존 → 함께 정정.
- 결정: 따옴표 제거, 표의 두 컬럼 모두 정정.

### 5. L46 budget hard-stop 범위 — 추적축과 집행 scope 혼동
- 추적축 (cost_events.ts:9-30): companyId / agentId / issueId / projectId / goalId / heartbeatRunId / provider / biller / billingType / model.
- hard-stop 정책 평가 (budgets.ts:655): `inArray(budgetPolicies.scopeType, ["company", "agent", "project"])` — 3 scope 만.
- 시작 차단 (budgets.ts:716-862 getInvocationBlock): company / agent / project 3 scope 동일.
- 큐/실행 취소 (heartbeat.ts:9431-9456 cancelBudgetScopeWork): agent / company / project 3 scope 동일.
- R01 본문은 "company/agent/project/goal/issue/provider/model 차원으로 추적하고 초과 시 ... 일시정지·큐 취소" 로 두 개념을 한 문장에 섞었음 + heartbeatRun 빠지고 attribution 축 일부 누락.
- 결정: 추적축과 집행 scope 를 두 절로 분리하고 `path:line` 인용 추가.

## REJECT 0건

## DEFER 0건
