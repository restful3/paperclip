# 챕터 01 — 라운드 2 변경 요약

## 적용 (2 ACCEPT)

### R2-A1. 표 1 ui/cli/db 의존 정정
- `ui/`: `shared` 만 → `shared · 9개 adapter workspace · adapter-utils · hermes-paperclip-adapter` + `ui/package.json:37-54` 인용
- `cli/`: `shared · server` 만 → `shared · server · db · adapter-utils · 9개 adapter workspace` + `cli/package.json:40-57` 인용
- `packages/db`: `embedded-postgres` 만 → `shared · drizzle-orm · embedded-postgres · postgres` + `packages/db/package.json:46-50` 인용
- 표 제목도 "패키지별 역할과 의존" → "패키지별 역할과 주요 런타임 의존" 으로 정밀화

### R2-A2. 빌드 명령 정정
- 본문 `:73`: `pnpm -r build` 단독 → 루트 `pnpm build` 가 preflight + `pnpm -r build` 순서로 실행한다고 명시(`package.json:16`)
- 표 2: `pnpm -r typecheck` / `pnpm -r build` → `pnpm typecheck` / `pnpm build` (preflight 포함 진입점)

## DEFER (수용)
- D1. "위에서 아래로만 흐른다" — 표 1 갱신 후 후속 재판단
- D2. 한국어 조사 띄어쓰기 미세 — 핵심 사실 우선

## 변경 정량
- 추가 변경 \~10줄. Ch01 누적 \~40줄. 전체 누적 \~72 / 200 checkpoint.

## 다음
라운드 3 검토 요청. handout 컨텍스트 이미 새 세션에 주입됨.
