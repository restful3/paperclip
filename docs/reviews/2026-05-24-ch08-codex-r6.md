# ch08 Round 6 — Codex Review

_2026-05-24, codex / R6 / 1 정합성 finding_

## F1 — line 90: `reports_to` 설명이 현재 구현보다 강하게 단정됨

- **현재 본문 인용**: `reports_to | (없음 — CEO 만 NULL) | 두 번째부터 picker 활성화`
- **판정**: 현재 구현과 불일치.
- **제안 수정안**: `reports_to | (첫 에이전트는 없음) | 두 번째부터 picker 활성화. 현재 구현은 No manager 선택도 허용하므로 CEO-only root 는 운영 규칙/향후 invariant 로 봐야 한다.`
- **근거**: `/agents/new`는 첫 에이전트일 때만 `ReportsToPicker`를 disabled 처리하고(`ui/src/pages/NewAgent.tsx:96-111`, `:251-256`), 후속 에이전트에서는 picker가 활성화된다. 그러나 picker 자체에는 `No manager` 선택지가 있어 `null`을 명시적으로 저장할 수 있다(`ui/src/components/ReportsToPicker.tsx:79-90`). 서버 create/update validator도 `reportsTo`를 optional/nullable 로 허용하며(`packages/shared/src/validators/agent.ts:66-79`), agent service는 값이 있을 때 manager/company/cycle 만 검증한다(`server/src/services/agents.ts:281-299`, `:347-351`, `:414-416`). 따라서 현재 구현 기준으로는 "CEO 만 NULL" 이라고 단정하면 안 된다.

## R5 확인

- `server/src/routes/access.ts:2454-2495` → `server/src/routes/access.ts:2408-2453` 정정은 `docs/report/08-usage.md:192`에 반영되어 있음.
