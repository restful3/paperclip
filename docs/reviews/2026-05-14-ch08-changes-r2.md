# Ch08 — 라운드 2 변경 요약

## 적용한 ACCEPT (4건)

| 항목 | 수정 위치 (대략) | 변경 골자 |
|---|---|---|
| 8-15 §4 회사 설정 주석 | line 81 부근 (회사 설정 단락) | 월 예산을 `/company/settings` 항목에서 분리, `PATCH /companies/:companyId/budgets` + `/costs` 경로로 이동 |
| 8-16 §8 단독 docker run | line 160 부근 코드 5 | `docker run` 라인에 `-e BETTER_AUTH_SECRET=$(openssl rand -hex 32)` 추가, `Dockerfile:76` 기본값 인용 |
| 8-17 §9 코드 6 | line 175 부근 | `PAPERCLIP_DEPLOYMENT_MODE=authenticated` + `PAPERCLIP_DEPLOYMENT_EXPOSURE=public` 두 줄 추가, 본문에 기본값 fallback 노트(`server/src/config.ts:160-180`) |
| 8-18 §9 board-claim | line 185 부근 | "보드 첫 사용자 승격" 일반화 → "local_trusted → authenticated 전환 시 instance admin 이 local-board 한 명일 때만" 조건부 흐름으로 정정 |

## REJECT / DEFER — 변동 없음

## 다음

`docs/reviews/2026-05-14-ch08-codex-r3.md` 로 재리뷰 요청.
