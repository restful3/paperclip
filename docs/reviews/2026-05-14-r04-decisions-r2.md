# R04 라운드 2 결정

- ACCEPT 1 / REJECT 0 / DEFER 0

### 1. L51 Pi `buildArgs` — extraArgs/userPrompt 위치 명확화
- 코드 (`pi-local/src/server/execute.ts:632-641`): `args.push(...extraArgs)` 그 다음 `args.push(userPrompt)`. extraArgs 는 임의 추가 인자, userPrompt 만 마지막 positional.
- r1 수정문은 둘을 "마지막 positional 로 ... 이 순서로" 라고 표현해 extraArgs 도 positional 처럼 읽힘.
- 결정: extraArgs 와 userPrompt 의 의미·위치 분리해 표기.
