# R03 라운드 1 변경 요약

대상: `docs/research/03-claude-code-cli.md`. ACCEPT 4/4 반영. REJECT 0, DEFER 0.

## 변경 항목

### 1. L15 — curl `-fsSL` 추가
- AS-IS: `curl https://claude.ai/install.sh | bash`
- TO-BE: `curl -fsSL https://claude.ai/install.sh | bash`

### 2. L24 — "headless mode" 직접 인용 제거
- AS-IS: `공식 문서는 "CLI는 이전에 'headless mode'로 불렸으며, -p 플래그와 모든 CLI 옵션은 동일하게 작동한다"고 명시한다`
- TO-BE: `공식 문서는 모든 CLI 옵션이 -p 와 함께 동작한다고 설명한다`

### 3. L40 — skills 분류 정정
- AS-IS: "slash commands(`/commit` 등) 와 사용자 정의 skills 는 인터랙티브 모드 전용이며..."
- TO-BE: 두 경우 분리 — "`/commit` 처럼 사용자가 slash 로 호출하는 **skills 와 built-in commands** 는 인터랙티브 모드 전용 / 일반 skills·hooks·plugins·MCP·auto memory·`CLAUDE.md` 자동 로드는 `-p` 에서도 동작하며 `--bare` 가 이를 끈다"

### 4. L75 — parse.ts 세 신호 분리
- AS-IS: 세 신호 (usage limit / transient / login) 를 모두 `retryNotBefore` 트리거로 묶어 표기
- TO-BE: 세 함수와 분리된 매칭으로 명시 — `extractClaudeRetryNotBefore` / `isClaudeTransientUpstreamError` / `detectClaudeLoginRequired` 각각 인용 (`parse.ts:9-15`, `:132-149`, `:355-391`)

## 변경된 파일
- `docs/research/03-claude-code-cli.md` (4 라인 수정)
