# R04 라운드 1 결정

- ACCEPT 4 / REJECT 0 / DEFER 0

## ACCEPT 4건

### 1. L33 Cursor 블로그 인용 — 출처에 없는 specifics
- WebFetch: 2025-08-07 블로그. "January 2026" / "Background Agents API tied to CLI key" / "Free Plan headless CLI only" 모두 본 URL 미확인.
- 결정: 블로그가 직접 지원하는 범위로 축약.

### 2. L41 OpenCode build/plan — 출처 보강
- WebFetch (https://opencode.ai/docs/agents/): Build 는 "default primary agent with all tools enabled", Plan 은 "restricted agent ... file edits·bash 가 ask 로 제한". 둘 다 명시 확인.
- 결정: 별도 인용 [OpenCode Agents] 추가.

### 3. L51 tool call 일반화 정정
- 코드: cursor-local/parse.ts (52-149) — `usage`/`sessionId`/`costUsd`/`summary`/`errorMessage` 만 반환. codex-local/parse.ts (14-70) — `sessionId`/`summary`/`usage`/`errorMessage`. opencode-local/parse.ts (22-87) — `toolErrors` 분리. pi-local/parse.ts (14-31) — `toolCalls` 별도 반환.
- 결정: 공통 보장(usage/cost/session/summary) 과 보조 파싱(toolCalls/toolErrors) 을 분리.

### 4. L51 Pi `buildArgs` 인자 ordering — 사용자 프롬프트 누락 + 조건부 인자 표기
- 코드 (`pi-local/src/server/execute.ts:618-641`): 조건부 `--provider/--model/--thinking`, 마지막 positional `extraArgs` + `userPrompt`. R04 본문은 사용자 프롬프트가 빠지고 조건부도 항상으로 표기됨.
- 결정: 조건부 표시 + extraArgs/userPrompt 추가.

## REJECT 0건

## DEFER 0건
