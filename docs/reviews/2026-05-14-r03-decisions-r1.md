# R03 라운드 1 결정

- ACCEPT 4 / REJECT 0 / DEFER 0

## ACCEPT 4건

### 1. L15 curl 명령 `-fsSL` 누락
- 원본 (overview): "curl -fsSL https://claude.ai/install.sh | bash"
- 본문은 `-fsSL` 빠진 형태로 인용. 검증 가능한 명령을 직접 표기할 거면 옵션을 정확히.
- 결정: `-fsSL` 추가.

### 2. L24 "이전에 headless mode 로 불렸다" 표현 부재
- WebFetch 결과: headless 문서는 페이지 제목이 "Run Claude Code programmatically" 이고, 본문에서 "All CLI options work with `-p`" 는 확인 가능. 그러나 "이전에 'headless mode'로 불렸으며" 문장은 본 페이지에서 확인되지 않음.
- 결정: 직접 인용 제거 → 문서가 실제로 지원하는 범위로 paraphrase.

### 3. L40 skills 인터랙티브 전용 과잉 일반화
- 원본 (headless): "User-invoked skills like /commit and built-in commands are only available in interactive mode."
- 추가: `--bare` 가 hooks · skills · plugins · MCP · auto memory · CLAUDE.md 자동 탐색을 끔.
- 본문은 "사용자 정의 skills 는 인터랙티브 모드 전용" 으로 일반화 → 부정확. 실제는 user-invoked slash skill + built-in command 만 전용, 일반 skill 자동 로드는 `--bare` 여부에 따라 결정.
- 결정: 두 케이스를 분리해 정정.

### 4. L75 parse.ts 세 retry 신호 혼동
- 코드 (parse.ts):
  - `extractClaudeRetryNotBefore` (L355-368) — `CLAUDE_EXTRA_USAGE_RESET_RE` (5-hour/weekly/usage reset) 만 매칭해 `retryNotBefore` 시각 계산
  - `isClaudeTransientUpstreamError` (L370-391) — `CLAUDE_TRANSIENT_UPSTREAM_RE` (rate-limit/429/503/529/overloaded/throttle 등) 매칭. login required 케이스는 제외
  - `detectClaudeLoginRequired` (L132-149) — `CLAUDE_AUTH_REQUIRED_RE` 별도 매칭, auth-required 신호
- 본문은 세 신호를 모두 `retryNotBefore` 단일 결정으로 합쳐 표기 → 부정확.
- 결정: 세 함수와 정규식을 분리해 명시.

## REJECT 0건

## DEFER 0건

(참고로 L75 직전 줄의 `**billing type(...)**` bold 가 닫는 `**` 뒤에 `를` 한글이 바로 붙어 CommonMark 규칙상 렌더 안 됨. 별도 라인 ±2 nit 라 DEFER. 후속 라운드 일괄 정합화 후보.)
