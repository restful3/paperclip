라운드 1의 주요 수정은 반영됐지만, Pi `buildArgs` ordering 정정 문장에 아직 작은 사실 혼동이 남아 있습니다. 인자 순서를 검증 대상으로 둔 항목이라 substantive 수정으로 보는 편이 안전합니다.

### 1. Pi `extraArgs`와 `userPrompt` 위치 표현 혼동

**위치**: L51

**현재**: ``--tools` · `--session` · `--skill` 을 붙이고 마지막 positional 로 `extraArgs` 와 사용자 `userPrompt` 를 이 순서로 조립한다.`

**문제**: [ACCEPT] 사실 정확성 문제. 실제 `buildArgs`는 `--skill` 뒤에 `extraArgs`를 먼저 spread하고, 그 다음 `userPrompt`를 마지막 positional argument로 추가합니다(`packages/adapters/pi-local/src/server/execute.ts:632-641`). 따라서 “마지막 positional로 `extraArgs`와 `userPrompt`”라고 쓰면 `extraArgs`까지 positional prompt처럼 읽힙니다. `extraArgs`는 임의 추가 CLI 인자이고, 마지막 positional은 `userPrompt` 하나입니다.

**제안**: “`--tools` · `--session` · `--skill` 뒤에 `extraArgs`를 붙이고, 마지막 positional argument로 `userPrompt`를 추가한다”로 고치세요.

라운드 2 요약 — ACCEPT 1건 / REJECT 0건 / DEFER 0건.
