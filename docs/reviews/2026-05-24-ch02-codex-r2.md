# ch02 `02-data-model.md` Round 2 리뷰 — Codex

## 1.

**위치**  
`docs/report/02-data-model.md:11`

**현재 본문 인용**  
> 보드 운영 흐름을 따라가는 데 필수적인 12\~13개 테이블만 추려 그림 4로 묶었다.

**문제 카테고리**  
정합성

**제안 수정안**  
> 보드 운영 흐름을 따라가는 데 필수적인 핵심 테이블과 보조 테이블 묶음을 추려 그림 4로 묶었다.

또는 수치를 유지하려면:

> 보드 운영 흐름을 따라가는 데 필수적인 12개 이상 테이블을 추려 그림 4로 묶었다.

**근거**  
실제 `assets/fig-04-er-core.svg` 는 `companies`, `agents`, `goals`, `projects`, `approvals`, `issues`, `heartbeat_runs`, `issue_comments`, `issue_documents`, `issue_attachments + work_products`, `cost_events`, `budget_policies`, `budget_incidents`, `activity_log`, `agent_api_keys · board_api_keys · agent_runtime_state` 를 표시한다(`docs/report/assets/fig-04-er-core.svg:11`, `:20`, `:31`, `:40`, `:49`, `:58`, `:71`, `:82`, `:87`, `:91`, `:96`, `:108`, `:114`, `:119`, `:126`). 묶음 박스를 어떻게 세어도 12~13개로 고정하기 어렵고, 개별 테이블명 기준으로는 13개를 넘는다. 같은 문서의 그림 캡션은 이미 "12개 이상 테이블"이라고 쓰고 있어(`docs/report/02-data-model.md:13`) 본문도 그 수준으로 맞추는 편이 안전하다.
