# 챕터 00 — 라운드 2 변경 요약

라운드 1 검토로부터 받은 5개 ACCEPT 항목을 모두 반영. DEFER 2건은 그대로 보류.

## 적용

### 1) §1 본문 CJK bold 공백 (R2-A3, :13)
- `**...회사 운영체제**다` → `**...회사 운영체제** 다`

### 2) §3 그림 1 캡션 CJK bold 공백 (R2-A3, :30)
- `**그림 1. Paperclip의 3-zone 큰 그림**은` → `**그림 1. Paperclip의 3-zone 큰 그림** 은`

### 3) §3 그림 1-A → 그림 2 (R2-A4)
- 본문 참조: "그림 1-A가 그 원형이다" → "그림 2가 그 원형이다"
- 캡션: "**그림 1-A. Orchestrator-workers 패턴 ...**" → "**그림 2. Orchestrator-workers 패턴 ...**"

### 4) §4 표 1 원칙 #4 셀: 원칙 + 구현 분리 (R2-A1)
- 셀 안에서 "원칙: 모든 issue ... 도달해야 한다. 구현: `goal_id`/`parent_id`가 nullable이며 fallback이 회사 기본 goal로 채우려 시도한다." 두 줄로.

### 5) §4 코드 매핑 단락: line citation 정밀화 + 중복 문장 제거 (R2-A5)
- `issue-goal-fallback.ts` → `:3-12` + `resolveIssueGoalId` 명시
- `doc/execution-semantics.md`의 8\~12장 → `:105` (stranded reconciliation) / `:174-197` (명시 recovery action) / `:207-245` (stranded 신호) 세 인용으로 교체
- 표 셀에서 이미 nullable/fallback 설명을 했으므로 단락 끝 "원칙은 슬로건이고 구현은 fallback이 있는 nullable" 중복 한 문장 제거

### 6) §5 V1 Out 해설: SPEC 인용 분리 (R2-A2) + CJK bold 공백 (R2-A3, :73)
- `doc/SPEC.md:515-517` 단일 인용 → KB는 `doc/SPEC.md:491` + `:500-502`, 매출 회계는 `:493` + `:517`, ClipHub는 `doc/SPEC-implementation.md:80`, governance/self-healing은 `:81-82` 로 분리
- `**KB · 매출 회계 · 공개 마켓플레이스는 의도적으로 코어 밖**이다` 구문 자체를 재작성("Out 항목의 성격은 균질하지 않다") — CJK bold 위반 해소 + 표현 명확화

## DEFER (재확인)

- 라운드 1 1건 + 라운드 2 2건 = 합쳐서:
  - "orchestrator-workers 일반화" 톤 — 전체 리포트 톤 합의 후 일괄
  - "80여/약 60개" 테이블 수치 — 자동 산출 어려움
- 모두 챕터 00 라운드에서 의도적으로 미적용.

## 변경 정량

- 챕터 00 LOC: 87 → 88 (+1). 실질 수정 \~12줄.
- 누적 챕터 00 변경: ~32줄 (라운드 1: ~20 + 라운드 2: ~12)
- 누적 200 LOC checkpoint 까지 여유

## 다음 라운드 요청

라운드 3 검토 요청. R2 결정 로그(`docs/reviews/2026-05-14-ch00-decisions-r2.md`)와 변경 요약(이 파일)을 함께 봐주세요. 더 이상 ACCEPT 가능한 항목이 없다면 첫 줄 `===CODEX_FINAL_APPROVAL===`.
