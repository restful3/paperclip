# R09 라운드 1 결정

- ACCEPT 5 / REJECT 0 / DEFER 0

### 1. L13 SSE 지수 백오프 단정 — 부분 정정
- WebFetch: WHATWG 는 "user agents might introduce an exponential backoff delay" 라고 허용 (may, not must). MDN 은 미언급.
- 결정: "지수 백오프 지연 도입 허용 (표준 의무는 아님)" 으로 약화.

### 2. L41 Anthropic 직접 인용
- WebFetch: "operate autonomously over multiple turns" 표현 부재. 실제는 "potentially operate for many turns", "Once the task is clear, agents plan and operate independently".
- 결정: 원문 표현으로 교체.

### 3. L47 adapter.execute vs adapter.invoke
- 코드 (`heartbeat.ts:7662`): `adapter.execute({...})` 가 실제 호출. `adapter.invoke` 는 같은 파일 :7636 에서 eventType 문자열로 등장.
- scheduler 흐름: 직접 호출 아닌 wakeup enqueue → run claim → execute.
- 결정: 정확한 메서드명·흐름 + 이벤트 타입명 분리.

### 4. L47 silent-run watchdog 시그널
- 코드 (`recovery/service.ts:676-683`, `:726-760`, `:1156-1167`): `lastOutputAt` / `processStartedAt` / `startedAt` / `createdAt` 우선순위로 silence age 계산.
- Ch03 합의: "출력 silence 가 진짜 평가 신호".
- 결정: "heartbeat 신호 없이" → "lastOutputAt 기준 출력 silence" 정정 + recovery/service.ts 인용.

### 5. L47 execution-semantics 경로
- ls: `docs/execution-semantics.md` 없음, `doc/execution-semantics.md` 존재.
- 결정: 경로 정정.
