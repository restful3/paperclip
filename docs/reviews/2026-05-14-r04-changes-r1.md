# R04 라운드 1 변경 요약

대상: `docs/research/04-headless-coding-agents.md`. ACCEPT 4/4 반영.

### 1. L33 Cursor 블로그 인용 축약
- AS-IS: "1월 2026 이후 동일 토큰으로 IDE Agent와 헤드리스 CLI 둘 다 호출되며, 별도의 Background Agents API 도 같은 키로 묶여 있다(단 Free Plan 키는 헤드리스 CLI만 허용)"
- TO-BE: "Cursor Agent CLI 는 Cursor 구독의 일부로 editor 외부(터미널·CI) 에서 동일한 Cursor Agent 와 구독 모델을 그대로 호출할 수 있게 해주는 도구로 출시됐다" ([Cursor blog 2025-08-07])

### 2. L41 OpenCode build/plan — 별도 출처 추가
- AS-IS: 단순 build/plan 분리 진술, 인용은 OpenCode MCP 로 묶임
- TO-BE: Build/Plan 두 primary agent + Plan 의 `ask` 제한 명시 + [OpenCode Agents] 인용 추가

### 3. L51 공통 어댑터 보고 일반화 정정
- AS-IS: "JSONL stdout을 파싱해 token usage·툴 호출·세션 ID를 추출해..."
- TO-BE: 공통(usage/cost/session/요약/오류) 과 보조(pi-local toolCalls / opencode-local toolErrors) 분리, `parse.ts` 라인 인용

### 4. L51 Pi `buildArgs` 인자 ordering 정정
- AS-IS: 조건부 인자를 항상 들어가는 것으로 표기 + 마지막 사용자 프롬프트 누락
- TO-BE: 항상/조건부 구분 + `extraArgs` + `userPrompt` 마지막 positional 추가, `execute.ts:618-641` 인용

## 변경된 파일
- `docs/research/04-headless-coding-agents.md` (4 라인 수정)
