# ch03 Round 1 — Changes Applied

_2026-05-24, codex-a / Round 1 → 2 송부용_

## docs/report/03-runtime-execution.md — 9곳 수정

```diff
- t₄에 토큰 사용량이 cost event로 들어와 회사·에이전트·이슈 차원으로 롤업되고, 이 변화는 회사 단위 WebSocket 라이브 채널(...)로 보드 UI에 즉시 반영된다.
+ t₄에 토큰 사용량이 cost event로 기록되고, 회사·에이전트의 월간 spend가 갱신된다. 이슈·프로젝트·run은 cost event의 귀속 축으로 남는다. 관련 실행 상태와 예산 임계값 변화는 각각 `heartbeat.run.*` 또는 `activity.logged` 라이브 이벤트로 보드 UI에 반영된다.

- `adapter.execute(ctx)` 로 들어간다(`heartbeat.ts` 의 invocation 경로).
+ `adapter.execute(ctx)` 로 들어간다(`heartbeat.ts:7784-7808`).

- (`doc/SPEC-implementation.md:421-426`)
+ (`doc/SPEC-implementation.md:423-432`)

- `doc/execution-semantics.md` §7 이 강조하는 **liveness 계약**
+ `doc/execution-semantics.md:155-172` 가 강조하는 **liveness 계약**

- `doc/execution-semantics.md` §8 은 두 가지 stranded 패턴을 정의한다.
+ `doc/execution-semantics.md:301-329` 는 두 가지 stranded 패턴을 정의한다.

- 이는 §12 가 명시한 의도다 — "... create explicit recovery action ..."
+ 이는 `doc/execution-semantics.md:449-464` 가 명시한 보수적 회복 모델이다 — "... open an explicit recovery action ..."

- > "Suspicious silence creates a medium-priority review issue. ..." — `doc/execution-semantics.md` §10
+ > Suspicious silence 는 selected recovery owner 에게 medium-priority watchdog recovery action 을 만들고 ... (`doc/execution-semantics.md:357-376`).

- API 서버가 부팅되면 startup/periodic reconciliation 이 결정론적 순서로 돈다(`index.ts:676-780`).
+ API 서버가 부팅되면 heartbeat startup reconciliation 이 결정론적 순서로 돈다(`index.ts:723-760`). periodic recovery loop 도 같은 핵심 순서를 반복한다(`index.ts:789-826`).

- 이 보수적 입장이 §12의 결론이다.
+ 이 보수적 입장은 `doc/execution-semantics.md:449-464` 의 결론이다.
```

## 누적 LOC delta (그룹 A)

- ch00: 7 / ch01: ~14 / ch02: ~16
- ch03 R1: ~20
- **그룹 A 누적: ~57 LOC** (LOC_CHECKPOINT 200 까지 143 LOC 여유)
