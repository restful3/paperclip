# ch04 Round 1 — Changes Applied

_2026-05-24, codex-a / Round 1 → 2 송부용_

## docs/report/04-adapters-and-skills.md — 9곳 수정

```diff
- (registry.ts:511-524)
+ (registry.ts:513-525)

- local coding adapters(claude-local, codex-local, cursor-local, gemini-local, opencode-local, pi-local)
+ local coding adapters(claude-local, codex-local, cursor-local, gemini-local, grok-local, opencode-local, pi-local)

- (registry.ts:128-138, 488)
+ (registry.ts:128-138, 497)

- 표 1. 런타임 전용 built-in 10종 + 범용 2종
+ 표 1. 런타임 전용 built-in 11종 + 범용 2종

(표 1 — gemini-local 다음에 grok-local 행 신규 추가)
+ | grok-local | xAI Grok Build CLI | grok --output-format streaming-json --single … | ● | proprietary xAI/Grok service · account/API billing |

- cursor-local: closed-source · BYOK
+ cursor-local: closed-source · Cursor subscription/usage billing · BYOK option
- cursor-cloud: proprietary · BYOK
+ cursor-cloud: proprietary · Cursor API key / subscription usage

- 폐쇄형(Cursor)부터 Apache-2.0... 표준 명세(ACP), 오픈 런타임(OpenClaw), 별도 npm 패키지로 들어오는 Hermes
+ 폐쇄형(Cursor), proprietary service 계열(Grok Build), Apache-2.0... 표준 명세(ACPX), 오픈 런타임(OpenClaw), 별도 npm 패키지로 들어오는 Hermes

- 현재 upstream master의 registry.ts:128~138은 hermes-paperclip-adapter를 정적 import하고, 같은 파일의 built-in 등록 배열에 hermesLocalAdapter를 포함한다.
+ 현재 upstream master 는 hermes-paperclip-adapter 를 server/package.json:71 의 npm dependency 로 두고, registry.ts:128-138 에서 정적 import 하며, built-in 등록 배열에도 hermesLocalAdapter 를 포함한다(registry.ts:523).

- sandbox-providers/ 에는 4종이 들어 있다(e2b, cloudflare, daytona, exe-dev). ...exe.dev VM 어디서든
+ sandbox-providers/ 에는 5종이 들어 있다(e2b, cloudflare, daytona, exe-dev, modal). ...exe.dev VM·Modal sandbox 어디서든

- plugin.ts:246-269 + :624-628, sdk/index.ts:260-273, sdk/types.ts:852-862
+ plugin.ts:247-268 + manifest :640-648 + capability :737-745, sdk/index.ts:286-299, sdk/types.ts:919-929
```

## 누적 LOC delta (그룹 A)

- ch00: 7 / ch01: ~14 / ch02: ~16 / ch03: ~20
- ch04 R1: ~22 (F3 행 추가 + 표현 변경 큼)
- **그룹 A 누적: ~79 LOC** (LOC_CHECKPOINT 200 까지 121 LOC 여유)
