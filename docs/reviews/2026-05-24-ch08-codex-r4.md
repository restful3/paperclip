# ch08 Round 4 리뷰 — 08-usage.md

## 정합성

### F1

- **위치**: line 106
- **현재 본문 인용**: `현재 보드 UI/API 는 전체 org tree 의 에이전트를 직접 조회·생성·수정할 수 있다(\`ui/src/App.tsx:87-96\`, \`server/src/routes/agents.ts:571-580\`).`
- **문제 카테고리**: 정합성
- **제안 수정안**: 인용을 `ui/src/App.tsx:87-96`, `server/src/routes/agents.ts:571-580`, `server/src/routes/agents.ts:2540-2548`, `server/src/routes/agents.ts:677-685`로 보강.
- **근거**: 현재 인용된 `server/src/routes/agents.ts:571-580`은 `assertBoardCanManageAgentsForCompany()`이며 내부 action은 `agents:create`라서 "생성" 근거로는 맞지만 "수정" 근거까지는 직접 보여 주지 않는다. agent 수정 route는 `router.patch("/agents/:id", ...)`에서 기존 agent를 찾고 `assertCanUpdateAgent()`를 호출한다(`server/src/routes/agents.ts:2540-2548`), 실제 update 권한 검사는 `agent_config:update` action으로 수행된다(`server/src/routes/agents.ts:677-685`).

## 표현

이상 없음

## 품질

이상 없음
