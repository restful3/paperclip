# R02 라운드 1 결정

- ACCEPT 4 / REJECT 0 / DEFER 0

## ACCEPT 4건

### 1. L13 ChatGPT MCP 도입 — InfoQ 인용으로 뒷받침 안 되는 specifics
- WebFetch 결과: InfoQ 기사 (2025-10-13)는 "rolled out full MCP support" + "now in beta under Developer Mode" 만 진술. 2025-09-10 도입일 / 2025년 3월 OpenAI 채택 모두 본 URL에서 확인 불가.
- 결정: 두 specifics 제거. InfoQ가 실제로 지원하는 문장으로 축약. (날짜 정확성을 유지하고 싶으면 별도 OpenAI 출처를 후속 라운드에서 추가하는 옵션.)

### 2. L43 Streamable HTTP 보안 — MUST/SHOULD 강도 분리
- 원문 (https://modelcontextprotocol.io/docs/concepts/transports):
  - "Servers **MUST** validate the `Origin` header on all incoming connections to prevent DNS rebinding attacks"
  - "When running locally, servers **SHOULD** bind only to localhost (127.0.0.1) rather than all network interfaces"
- R02 본문은 둘을 "요구한다"로 묶어 SHOULD를 MUST로 격상시킴.
- 결정: 두 규범을 두 절로 분리, MUST/SHOULD 명시.

### 3. L51 Zylos 수치 — 본문 인용과 원문 수치 불일치
- WebFetch 결과: Zylos 글 (2026-03-08) 은 "Community registries now index over 18,000 MCP servers" + 표 (Glama.ai 18,374 / MCP.so 18,250+) 만 제시. R02의 "2025 Q1 1,200 → 2026년 4월 9,400+" 수치는 본 URL에 없음.
- 결정: Zylos 실제 수치로 교체 + 날짜 2026-03 으로 정정.

### 4. L57 Paperclip MCP — 어댑터 vs MCP client 책임 경계 혼동
- 코드 (packages/mcp-server/src/index.ts:1-30):
  - `McpServer` + `StdioServerTransport` 로 standalone stdio MCP server 구축
  - `PaperclipApiClient` 로 REST 위임
  - tool 등록은 `createToolDefinitions(client)` 로 별도 모듈
- Ch04 (docs/report/04-adapters-and-skills.md:155): "두 층(SKILL.md + MCP)은 현재 어댑터 실행 계약(`execute` / `testEnvironment`) 과 **수직 직교** 한다"
- R02 L57 "어댑터 측은 표준 MCP 등록만 수행하면..." 은 주체가 잘못. 등록 주체는 MCP 호환 호스트/에이전트.
- 결정: 주체를 MCP 호스트/에이전트로 바꾸고 어댑터-MCP 직교 관계를 본문에서도 약하게 반영.

## REJECT 0건

## DEFER 0건
