# ch09 Round 1 리뷰 — 09-pros-cons.md

## 정합성

### F1

- **위치**: line 29
- **현재 본문 인용**: `**보수적 회복 모델** | \`doc/execution-semantics.md:407-422\` §12 + \`doc/SPEC-implementation.md:434-442\` V1 liveness rule`
- **문제 카테고리**: 정합성
- **제안 수정안**: `doc/execution-semantics.md:405-423` **§11** + `doc/SPEC-implementation.md:440-445` V1 liveness rule 로 수정.
- **근거**: 현재 `doc/execution-semantics.md:405`의 제목은 `## 11. Auto-Recover vs Explicit Recovery vs Human Escalation`이고, auto-recovery 보존/대체 agent 미선택 설명은 `:407-423`, 특히 `:419`에 있다. `doc/SPEC-implementation.md`의 V1 non-terminal liveness rule은 `:440-445`에 걸쳐 있으며, 현 인용 `:434-442`는 side effects 일부와 rule 일부만 포함한다.

### F2

- **위치**: line 31
- **현재 본문 인용**: `hard ceiling 정책(\`doc/SPEC.md:295-300\`) + \`budgetService.getInvocationBlock\`(\`server/src/services/heartbeat.ts:8610-8618\`) · \`withAgentStartLock\`(\`server/src/services/agent-start-lock.ts:32\`) · \`cancelBudgetScopeWork\`(\`server/src/services/heartbeat.ts:9431-9456\`) 세 메커니즘 조합`
- **문제 카테고리**: 정합성
- **제안 수정안**: `hard ceiling 정책(doc/SPEC.md:295-301) + budgetService.getInvocationBlock(server/src/services/budgets.ts:716-862; 호출 예 server/src/services/heartbeat.ts:3976-3982 또는 :8758) · withAgentStartLock(server/src/services/agent-start-lock.ts:32) · cancelBudgetScopeWork(server/src/services/heartbeat.ts:9579-9604)`로 수정.
- **근거**: hard ceiling 문장은 `doc/SPEC.md:301`에 있다. `getInvocationBlock` 정의는 `server/src/services/budgets.ts:716-862`이고, heartbeat 쪽에는 호출부가 있다(`server/src/services/heartbeat.ts:3976-3982`, `:8758`). `cancelBudgetScopeWork` 현재 정의는 `server/src/services/heartbeat.ts:9579-9604`다. `withAgentStartLock` 인용은 `server/src/services/agent-start-lock.ts:32`로 맞다.

### F3

- **위치**: line 33
- **현재 본문 인용**: `**풀 감사 로그 + WebSocket 라이브 피드** | \`activity_log\` 불변 + \`/api/companies/:companyId/events/ws\``
- **문제 카테고리**: 정합성
- **제안 수정안**: `activity_log append 중심 감사 로그 + /api/companies/:companyId/events/ws 기반 live event invalidation`처럼 "불변" 단정을 완화하고, 근거로 `packages/db/src/schema/activity_log.ts:6-26`, `server/src/services/activity-log.ts:65-87`, `server/src/realtime/live-events-ws.ts:63-65,208-211`, `ui/src/context/LiveUpdatesProvider.tsx:981-986`를 추가.
- **근거**: 일반 activity 기록은 `logActivity()`가 `activityLog`에 insert하고 live event를 publish한다(`server/src/services/activity-log.ts:65-87`). WebSocket path는 `server/src/realtime/live-events-ws.ts:63-65`에서 파싱하고, 이벤트는 `:208-211`에서 socket으로 전송된다. UI는 같은 path로 WebSocket을 연다(`ui/src/context/LiveUpdatesProvider.tsx:981-986`). 다만 activity_log를 완전 불변이라고 하기는 어렵다. agent 제거는 관련 activity row를 삭제하고(`server/src/services/agents.ts:501-518`), company 제거도 company activity를 삭제한다(`server/src/services/companies.ts:267-283`).

### F4

- **위치**: line 46
- **현재 본문 인용**: ``routes/issues.ts` 205 KB, `services/issues.ts` 201 KB, `services/company-portability.ts` 178 KB, `ui/pages/IssueDetail.tsx` 163 KB, `routes/access.ts` 140 KB, `routes/agents.ts` 119 KB, `routes/plugins.ts` 93 KB`
- **문제 카테고리**: 정합성
- **제안 수정안**: `ui/pages/IssueDetail.tsx`를 `ui/src/pages/IssueDetail.tsx`로 수정. 크기는 floor KiB 기준을 쓴다면 유지 가능하나, 필요하면 `ui/src/pages/IssueDetail.tsx`는 164 KB로 반올림 표기.
- **근거**: 실제 파일 경로는 `ui/src/pages/IssueDetail.tsx`다. 현재 크기는 `routes/issues.ts` 210,041 bytes(205 KiB), `services/issues.ts` 205,854 bytes(201 KiB), `services/company-portability.ts` 182,377 bytes(178 KiB), `ui/src/pages/IssueDetail.tsx` 167,634 bytes(163.7 KiB), `routes/access.ts` 143,747 bytes(140 KiB), `routes/agents.ts` 121,988 bytes(119 KiB), `routes/plugins.ts` 95,910 bytes(93 KiB)이다.

### F5

- **위치**: line 48
- **현재 본문 인용**: `multi-member boards · advanced governance 는 Not V1(\`doc/SPEC.md:489-493\`)`
- **문제 카테고리**: 정합성
- **제안 수정안**: `multi-member boards · advanced governance 는 Not V1(\`doc/SPEC.md:491-495\`, 또는 V1 구현 범위 기준 \`doc/SPEC-implementation.md:81\`)`로 수정.
- **근거**: `doc/SPEC.md:489-493`은 `Multiple Adapter types`와 `Not V1` heading 및 KB 항목까지만 포함하고, `Advanced governance models (hiring budgets, multi-member boards)`는 `doc/SPEC.md:494`에 있다. `doc/SPEC-implementation.md:81`도 `Multi-board governance or role-based human permission granularity`를 V1 밖으로 둔다.

### F6

- **위치**: line 53
- **현재 본문 인용**: `**모바일 UI 미완성** | "Mobile Ready" 슬로건 vs \`Inbox.tsx\` 101 KB의 데스크톱 위주 디자인`
- **문제 카테고리**: 정합성
- **제안 수정안**: `README.md:123-124`의 Mobile Ready 문구를 근거로 붙이고, `Inbox.tsx 101 KB`를 현재 크기인 `ui/src/pages/Inbox.tsx` 108 KiB(또는 decimal 111 KB)로 수정.
- **근거**: README의 `Mobile Ready` 문구는 `README.md:123-124`에 있다. 현재 `ui/src/pages/Inbox.tsx`는 110,152 bytes로 108 KiB(ceil) 또는 111 decimal KB다.

### F7

- **위치**: line 55
- **현재 본문 인용**: `**공식 클라우드 마켓플레이스 미정** | "ClipHub coming soon"`
- **문제 카테고리**: 정합성
- **제안 수정안**: `"ClipHub coming soon"` 직접 인용을 제거하고 `doc/SPEC-implementation.md:80` 또는 `doc/CLIPHUB.md:1-13,204-213` 근거로 바꾸기. 예: `Public marketplace (ClipHub)는 V1 밖(doc/SPEC-implementation.md:80), ClipHub는 별도 hosted registry 구상(doc/CLIPHUB.md:1-13,204-213)`.
- **근거**: 현재 repo에서 `"ClipHub coming soon"` 문자열은 본문/빌드 산출물 외에는 발견되지 않는다. V1 out 근거는 `doc/SPEC-implementation.md:80`이고, ClipHub의 별도 registry 구상은 `doc/CLIPHUB.md:1-13` 및 `:204-213`에 있다.

## 표현

이상 없음

## 품질

이상 없음
