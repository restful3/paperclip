# 챕터 07 리뷰 — 라운드 2

## ACCEPT 1

- 위치: `docs/report/07-governance-cost.md:78`
- 현재: 예산 파이프라인 2단계 `Normalization`이 “모델·공급자별 가격 표를 적용해 `cost_cents`로 환산”한다고 설명합니다.
- 문제: 현재 서버의 cost ledger는 모델/공급자별 가격표로 token usage를 가격 계산하지 않습니다. heartbeat 경로는 adapter result의 `costUsd`를 cents로 반올림해 `costEvents.costCents`에 넣고, public cost-event API도 caller가 `costCents`를 직접 제출합니다. 서버 cost service는 저장/집계/예산 평가를 수행하지, provider/model pricing table을 적용하는 normalization 계층이 아닙니다.
- 제안: “adapter/API가 보고한 `costUsd` 또는 `costCents`를 ledger의 `cost_cents`로 정규화·저장하고, 서버는 이를 집계·정책 평가한다”로 정정하세요. 가격표 기반 추정이 미래/연구 맥락이면 현재 구현과 분리하세요.
- 근거: `server/src/services/heartbeat.ts:1299`, `server/src/services/heartbeat.ts:1302`, `server/src/services/heartbeat.ts:6645`, `server/src/services/heartbeat.ts:6646`, `server/src/services/heartbeat.ts:6670`, `server/src/services/heartbeat.ts:6682`, `packages/shared/src/validators/cost.ts:4`, `packages/shared/src/validators/cost.ts:18`, `server/src/services/costs.ts:54`, `server/src/services/costs.ts:66`, `server/src/services/costs.ts:99`

## ACCEPT 2

- 위치: `docs/report/07-governance-cost.md:96`
- 현재: “위임이 가능하다 — CEO가 자기 직속 보고에 sub-budget을 잘라 줄 수 있다”고 설명합니다.
- 문제: 현재 예산 policy upsert, 회사 예산 PATCH, 에이전트 예산 PATCH, budget incident resolve 라우트는 모두 board 권한을 요구합니다. 에이전트/CEO가 hire approval payload에 `budgetMonthlyCents`를 담을 수는 있지만, 실제 agent budget policy 생성은 `hire_agent` approval이 board에 의해 승인된 뒤 approval service가 수행합니다. 즉 현재 구현 계약은 “CEO가 직접 sub-budget을 위임”하는 것이 아니라 “board가 예산을 설정하거나 board-approved hire payload가 agent budget policy가 됨”입니다.
- 제안: 해당 문장을 제거하거나 “agent budget은 board API 또는 board-approved hire approval을 통해 설정된다”로 정정하세요.
- 근거: `server/src/routes/costs.ts:247`, `server/src/routes/costs.ts:251`, `server/src/routes/costs.ts:259`, `server/src/routes/costs.ts:263`, `server/src/routes/costs.ts:280`, `server/src/routes/costs.ts:281`, `server/src/routes/costs.ts:314`, `server/src/routes/costs.ts:323`, `server/src/routes/agents.ts:2038`, `server/src/routes/agents.ts:2053`, `server/src/services/approvals.ts:143`, `server/src/services/approvals.ts:147`, `server/src/services/approvals.ts:155`

## ACCEPT 3

- 위치: `docs/report/07-governance-cost.md:126`, `docs/report/07-governance-cost.md:128`
- 현재: activity log의 가치로 “누가 언제 무엇을 바꿨나가 영원히 보존된다”고 쓰고, `activity_log`가 “불변”이며 정정은 새 row 추가로만 한다고 설명합니다.
- 문제: 일반 activity write 경로가 append 중심인 것은 맞지만, 현재 서비스에는 회사 삭제와 에이전트 삭제 시 관련 `activity_log` row를 삭제하는 cleanup 경로가 있습니다. 따라서 “영원히 보존” 또는 절대적 “불변”은 현재 구현 계약보다 강합니다.
- 제안: “일반 감사 이벤트는 append-only로 기록되며 정정은 새 row를 추가하는 방식이 원칙이다. 단, 회사/에이전트 삭제 cleanup에서는 관련 activity row가 제거될 수 있다”로 좁혀 쓰세요.
- 근거: `server/src/services/activity-log.ts:65`, `server/src/services/activity-log.ts:73`, `server/src/services/activity.ts:580`, `server/src/services/activity.ts:582`, `server/src/services/agents.ts:501`, `server/src/services/agents.ts:513`, `server/src/services/agents.ts:516`, `server/src/services/companies.ts:265`, `server/src/services/companies.ts:270`
