===CODEX_FINAL_APPROVAL===

# 챕터 04 리뷰 — 라운드 5

## 종합 판정

R4 ACCEPT 2건이 반영됐다. `supportsLocalAgentJwt`는 핵심 실행·모델·옵션 필드 범위로 이동했고, capability flags 예시는 실제 후반 필드에 맞게 정리됐다. Hermes `detectModel` 인용도 import와 adapter 등록 위치를 포함하는 범위로 수정됐다.

## ACCEPT-worthy

없음.

## 확인 근거

- `04-adapters-and-skills.md:13`: `ServerAdapterModule` 필드 범위가 `:349-396` / `:407-431`로 구분됨.
- `04-adapters-and-skills.md:19`: `getConfigSchema` 제공자가 `acpx-local` + `cursor-cloud`로 정정되고, Hermes `detectModel` 근거가 `server/src/adapters/registry.ts:116-123, 456-465`로 정리됨.
- `04-adapters-and-skills.md:111`: sandbox provider 4종과 실행 환경 명칭이 manifest 수준 설명과 충돌하지 않음.
- `04-adapters-and-skills.md:141`: plugin-managed skills 설명이 `skills.managed` capability, manifest `skills` 배열, SDK get/reconcile/reset API를 분리함.

## DEFER

- 그림 7 SVG 내부 `cursor_cloud` 행 추가/재배치는 기존 OUT-OF-SCOPE 유지.
- 외부 인용 그림 번호 `4-1~4` 통합은 글로벌 번호 정합화 단계에서 처리.

## OUT-OF-SCOPE

- PDF 재빌드.
