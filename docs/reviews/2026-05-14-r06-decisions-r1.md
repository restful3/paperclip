# R06 라운드 1 결정

- ACCEPT 3 / REJECT 0 / DEFER 0

### 1. Drizzle Kit 핵심 명령은 generate/migrate/push/pull
- WebFetch: 핵심 마이그레이션 워크플로는 generate/migrate/push/pull. studio 는 별도 UI 명령.
- 결정: 본문/표 모두 정정.

### 2. Paperclip 테이블 수 — 86 / 78
- 실측: `grep -h "pgTable(" packages/db/src/schema/*.ts | wc -l` = 86, schema 파일 = 78. Ch02 합의와 동일.
- 결정: "75 개 안팎" → "86 개 pgTable 정의 / 78 개 파일" 로 정정.

### 3. 마이그레이션 연결 결정 — drizzle.config.ts vs resolveMigrationConnection
- 코드: `packages/db/src/migrate.ts:5` `resolveMigrationConnection()` 호출. `migration-runtime.ts:183-193` 에서 fallback 체인 (DATABASE_URL → .paperclip env/config → embedded-postgres).
- `drizzle.config.ts` 는 Drizzle Kit 설정만 담당.
- 결정: 두 경로 분리 표기.
