# ch07 Round 1 — Changes Applied

_2026-05-24, codex-b / Round 1 → 2 송부용_

## docs/report/07-governance-cost.md — 10곳 수정

```diff
- V1의 보드는 *단일* 인간 운영자다(SPEC §10의 *Single human Board.*).
+ V1의 보드는 *단일* 인간 운영자다(`doc/SPEC-implementation.md:35` "Single human board operator per deployment"; `doc/SPEC.md:24` "V1: Single human Board.").

- `packages/shared/src/constants.ts:373~388` 가 단일 소스다
+ `packages/shared/src/constants.ts:408-422` 가 단일 소스다

- activation 서비스가 ... (`server/src/services/agents.ts:534-537`).
+ `hire_agent` approval 처리에서 `agentsSvc.activatePendingApproval()` 가 호출되어(`server/src/services/approvals.ts:102-117`), `agents.status='pending_approval'` row 만 `idle` 로 갱신해 heartbeat가 그 에이전트를 깨울 수 있게 만든다(`server/src/services/agents.ts:534-548`).

- 2. CEO 초기 전략 분해 — CEO가 첫 heartbeat에서 작성한 전략 트리(이슈 분해)를 보드가 `approvals.type='approve_ceo_strategy'`로 승인해야 다음 heartbeat가 돌기 시작한다.
+ 2. CEO 초기 전략 분해 — `approve_ceo_strategy` 는 V1 목표로 SPEC 에 정의되어 있으나(`doc/SPEC-implementation.md:67`, `:768-772`), 현재 코드에서는 approval type 상수만 존재한다 — CEO 초기 전략 분해를 보드가 게이트하는 흐름은 구현 대기 중이다.

- | 결정 override | ... | `POST /api/issues/:id/decisions` |
+ | 결정 override | ... | `PATCH /api/issues/:id` (`transition.decision` 시 자동 insert; watchdog 결정은 `POST /api/heartbeat-runs/:runId/watchdog-decisions`) |

- 가격 계산은 ... (`heartbeat.ts:1299-1302`, `validators/cost.ts:4-18`).
+ 가격 계산은 ... — adapter run 경로는 `normalizeBilledCostCents()` 가 costUsd→costCents 변환(`heartbeat.ts:1358-1361`, `:6764-6803` createEvent), API 보고 경로는 costCents 만 검증(`validators/cost.ts:4-23`, `routes/costs.ts:73-85`).

- `budget_incidents` 의 실제 컬럼은 ...
+ `budget_incidents` 의 핵심 컬럼은 ... (외 `id`/`company_id`/`policy_id`/`scope_type`/`scope_id`/timestamp 포함)

- (services/budgets.ts:213-253)
+ (services/budgets.ts:213-258)
- pending wakeup을 일괄 취소 (services/heartbeat.ts:9431-9456)
+ pending wakeup을 일괄 취소 (services/heartbeat.ts:9579-9604 cancelBudgetScopeWork() · helper :9431-9477)

- `server/src/services/costs.ts:454~492`
+ `server/src/services/costs.ts:454-502`

- (`agents.ts:501-516`, `companies.ts:265-270`)
+ (`agents.ts:501-518`, `companies.ts:267-283`)
```

## 누적 LOC delta (그룹 B)

- ch06: ~30 LOC (4 rounds, FINAL_APPROVAL)
- ch07 R1: ~20 LOC (10 Edit)
- **그룹 B 누적: ~50 LOC** (LOC_CHECKPOINT 200 까지 150 LOC 여유)
