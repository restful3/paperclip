# 챕터 10 리뷰 — 라운드 1

[10-1] 표 1 R9의 `recovery 4-pass` 는 현재 합의·코드와 맞지 않습니다.

- 본문 인용: `server/src/realtime/live-events-ws.ts` + heartbeat scheduler + recovery 4-pass
- 진실 소스: startup/periodic reconciliation은 `reapOrphanedRuns → promoteDueScheduledRetries → resumeQueuedRuns → reconcileStrandedAssignedIssues → reconcileIssueGraphLiveness → scanSilentActiveRuns → reconcileProductivityReviews` 흐름입니다(`server/src/index.ts:676-780`). HANDOFF도 startup reconciliation 7-pass로 합의했습니다(`docs/reviews/HANDOFF.md:66-69`).
- 분류: ❌ 사실 오류
- 권고: `recovery 4-pass` 를 `startup/periodic reconciliation 7-pass` 또는 `heartbeat recovery/liveness/watchdog reconciliation` 로 정정하세요.

[10-2] 표 1 R7의 `8개 패키지` 는 현재 pnpm workspace 규모와 맞지 않습니다.

- 본문 인용: `pnpm-workspace.yaml + 8개 패키지의 분할`
- 진실 소스: workspace globs는 `packages/*`, `packages/adapters/*`, `packages/plugins/*`, `packages/plugins/examples/*`, `server`, `ui`, `cli` 를 포함합니다(`pnpm-workspace.yaml:1-14`). 로컬 `pnpm -r list --depth -1 --parseable` 기준 루트 제외 workspace package는 24개이며, adapter package만 세도 9개입니다(`packages/adapters/claude-local/package.json:15-20`, `packages/adapters/codex-local/package.json:15-20` 등 동일 export 구조).
- 분류: ❌ 사실 오류
- 권고: 정확한 수치를 쓰려면 `24개 workspace 패키지` 로 바꾸거나, 수치가 필요 없으면 `다수 워크스페이스 패키지` 로 완화하세요.

[10-3] §4의 `3-메서드 + 4개 진입점` 은 장기 SPEC과 현재 구현 계약을 다시 섞습니다.

- 본문 인용: `어댑터 계약의 단순함 — 3-메서드 + 4개 진입점 조합`
- 진실 소스: 장기 SPEC은 `invoke/status/cancel` 3-method 계약입니다(`doc/SPEC.md:207-215`). 현재 구현의 `ServerAdapterModule` 필수 메서드는 `execute` 와 `testEnvironment` 두 개이며 나머지는 optional입니다(`packages/adapter-utils/src/types.ts:349-431`). 4개 진입점 자체는 adapter package `exports`의 `.`, `./server`, `./ui`, `./cli` 로 맞습니다(`packages/adapters/claude-local/package.json:15-20`; 설명은 `docs/report/04-adapters-and-skills.md:62-90`).
- 분류: ❌ 사실 오류
- 권고: `장기 SPEC의 3-method 계약 + 현재 구현의 2필수 메서드(ServerAdapterModule) + 4개 package exports 진입점` 처럼 두 층을 분리하세요.

[10-4] §3.1 그림 10-1\~10-4 도입 문장이 본문 그림 참조를 굵게 표시합니다.

- 본문 인용: `**그림 10-1. Augmented LLM** — 도구·리트리벌·메모리를 가진 단일 LLM.`
- 진실 소스: HANDOFF의 마크다운 관습은 캡션은 `**그림 N. 〈제목〉**`, 본문 그림 참조는 평문입니다(`docs/reviews/HANDOFF.md:102-107`). 본문은 line 46/52/58/64에서 도입 문장을 굵게 쓰고, 바로 아래 line 48/54/60/66에 별도 캡션을 다시 둡니다(`docs/report/10-research-map.md:46-66`). line 11의 `**그림 10** 이` 도 같은 본문 참조 위반입니다(`docs/report/10-research-map.md:11-13`).
- 분류: ⚠️ 마크다운 관습
- 권고: line 11과 line 46/52/58/64의 본문 참조는 평문으로 바꾸고, 굵은 캡션은 line 13/48/54/60/66에만 남기세요.

[10-5] 표 1 R10의 `budgets.ts (31 KB)` 와 `budget_incidents` 는 현재 코드와 맞습니다.

- 본문 인용: `cost_events 테이블 · budgets.ts (31 KB) · budget_incidents`
- 진실 소스: `server/src/services/budgets.ts` 는 31,698 bytes입니다. `budget_incidents` 테이블은 `packages/db/src/schema/budget_incidents.ts:7-42` 에 정의되어 있고, schema index에서도 export됩니다(`packages/db/src/schema/index.ts:16`).
- 분류: ✅ OK

ACCEPT 권장: [10-1], [10-2], [10-3], [10-4]
REJECT 후보: 없음
DEFER 요약: [10-5]는 재검증 OK. 외부 캡션 문체·라인 범위 미세 차이는 DEFER.
