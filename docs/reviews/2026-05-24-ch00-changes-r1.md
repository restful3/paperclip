# ch00 Round 1 — Changes Applied

_2026-05-24, 그룹 A·codex-a / Round 1 → 2 송부용 diff 요약_

## docs/report/00-overview.md (line 60)

### 변경 1 — 테이블/company scope 카운트

```diff
- 원칙 2는 **multi-company 스키마**로(80여 개 테이블 중 약 60개가 가진 `company_id` 컬럼)
+ 원칙 2는 **multi-company 스키마**로(88개 테이블 중 67개가 가진 `company_id` 컬럼)
```

근거: `packages/db/src/schema/*.ts` 직접 카운트 (`tables=88, company_id_tables=67`). CHANGE-IMPACT-2026-05-23.md:58 와 정합.

### 변경 2 — execution-semantics 인용 라인 정정

```diff
- 5는 [`doc/execution-semantics.md`](../../doc/execution-semantics.md)의 회복 규칙(stranded reconciliation `:105` · 명시 recovery action `:174-197` · stranded 신호 `:207-245`)으로 구현되어 있다
+ 5는 [`doc/execution-semantics.md`](../../doc/execution-semantics.md)의 회복 규칙(liveness contract `:155-172` · 명시 recovery action `:174-197` · stranded 신호 `:207-240`)으로 구현되어 있다
```

근거:
- `:105` 는 "stranded-work reconciliation **does not apply** to user-owned issues" 라는 예외 설명. 출처로 부적절.
- "stranded reconciliation" 키워드는 `:105`, `:347` 두 곳뿐 — 둘 다 출처 부적절.
- `:155-172` "Non-Terminal Issue Liveness Contract" 절이 회복 규칙 헌장.
- `:241` 부터 `### in_review` 새 섹션이라 `:207-245` 보다 `:207-240` 이 정확한 영역 (Agent-assigned todo/backlog/in_progress stalled 신호).

## DEFER 항목 (이번 라운드 미적용)

| Finding | 분류 | 처리 시점 |
|---|---|---|
| ch04 표 헤더 "10종" + grok-local 누락 | DEFER | 그룹 A·ch04 라운드 1 |
| ch00:13 슬로건 인용 위치 추가 | DEFER | codex 재지적 시 |

## 누적 LOC delta (그룹 A)

- 변경 파일: 1 (`docs/report/00-overview.md`)
- 변경 라인: 1 줄 안 두 곳 수정 → diff 기준 `-1, +1`
- **누적: -1 +1 = 2 LOC** (LOC_CHECKPOINT 200 까지 198 LOC 여유)
