# ch06 정합성/표현 리뷰 — Codex r1

대상: `docs/report/06-ui-and-board.md`

## 정합성

### 1. 라우팅 수와 "페이지" 표현이 섞여 있음

- **위치**: line 19, line 21
- **현재 본문 인용**: `## 2. 70+ 페이지의 라우팅 표`
- **문제 카테고리**: 정합성
- **제안 수정안**: 제목과 본문을 `70+ 라우트의 라우팅 표` 또는 `주요 페이지/라우트 표`로 낮추는 편이 안전합니다. 실제 `ui/src/pages/*.tsx` 비테스트 페이지 파일은 59개이고, `App.tsx`의 `<Route>` 엔트리는 리다이렉트/레이아웃 포함 129개라서 "70+ 페이지"는 무엇을 세는지 불명확합니다.
- **근거**: `ui/src/App.tsx:65-146`에 회사 prefix 아래 board route 묶음이 있고, `ui/src/App.tsx:276-334`에 글로벌/인스턴스/리다이렉트 route가 추가됩니다. 페이지 컴포넌트 import는 `ui/src/App.tsx:7-59` 범위입니다.

### 2. 회사 설정 경로 표기가 현재 라우트와 다름

- **위치**: line 28
- **현재 본문 인용**: ``/company/settings/{environments,access,invites,secrets}``
- **문제 카테고리**: 정합성
- **제안 수정안**: ``/company/settings/{environments,members,access,cloud-upstream,invites,secrets}`` 로 고치거나, `access`는 legacy route라고 명시하세요.
- **근거**: `ui/src/App.tsx:73-82`에서 `members`가 `CompanyAccess`의 현재 경로이고, `access`는 `CompanyAccessLegacyRoute`입니다. 같은 범위에 `cloud-upstream`, `:settingsRoutePath/*`도 있습니다.

### 3. `Secrets.tsx` LOC가 현재 코드와 다름

- **위치**: line 28, line 46, line 56
- **현재 본문 인용**: ``Secrets.tsx` (2,155 LOC)``
- **문제 카테고리**: 정합성
- **제안 수정안**: `Secrets.tsx` LOC를 `2,444 LOC`로 갱신하세요.
- **근거**: `ui/src/pages/Secrets.tsx:2444`가 파일 마지막 라인입니다.

### 4. `IssueRecoveryActionCard.tsx` LOC가 현재 코드와 다름

- **위치**: line 31
- **현재 본문 인용**: ``IssueRecoveryActionCard.tsx`(537 LOC)``
- **문제 카테고리**: 정합성
- **제안 수정안**: `537 LOC`를 `543 LOC`로 갱신하세요.
- **근거**: `ui/src/components/IssueRecoveryActionCard.tsx:543`이 파일 마지막 라인입니다.

### 5. 인박스 경로에서 `/inbox/blocked`가 빠져 있음

- **위치**: line 32
- **현재 본문 인용**: ``/inbox/{mine,recent,unread,all,requests}``
- **문제 카테고리**: 정합성
- **제안 수정안**: ``/inbox/{mine,recent,unread,blocked,all,requests}`` 로 갱신하세요. 필요하면 `/inbox/new`는 redirect라고 별도 표기하세요.
- **근거**: `ui/src/App.tsx:134-141`에 `mine`, `recent`, `unread`, `blocked`, `all`, `requests`, `new` redirect가 등록되어 있습니다.

### 6. 인스턴스 설정 표가 실제 하위 라우트를 과소 표기함

- **위치**: line 38
- **현재 본문 인용**: ``/instance/settings/adapters`, `/instance/settings/plugins`, `/instance/settings/plugins/:pluginId``
- **문제 카테고리**: 정합성
- **제안 수정안**: ``/instance/settings/{profile,general,access,heartbeats,experimental,plugins,plugins/:pluginId,adapters}`` 로 확장하거나, 현재 행을 `인스턴스 설정(어댑터/플러그인 발췌)`로 명시하세요.
- **근거**: `ui/src/App.tsx:291-300`에 `profile`, `general`, `access`, `heartbeats`, `experimental`, `plugins`, `plugins/:pluginId`, `adapters`가 등록되어 있습니다.

### 7. 최근 UI 확장 축 수가 본문 안에서 충돌함

- **위치**: line 52, line 61
- **현재 본문 인용**: `UI 표면이 다섯 축에서 동시에 확장됐다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `다섯 축`을 `여섯 축`으로 고치세요.
- **근거**: line 54-59의 bullet은 6개이고, `docs/report/CHANGE-IMPACT-2026-05-23.md:200`도 GAP-06을 6축으로 정리합니다.

### 8. locale JSON 개수 설명이 현재 디렉터리 상태와 어긋남

- **위치**: line 54
- **현재 본문 인용**: ``ui/src/i18n/locales/` 에 **39개 언어 JSON** 이 들어왔다`
- **문제 카테고리**: 정합성
- **제안 수정안**: `현재 locale JSON은 en 포함 40개이며, #6070에서 추가된 비영어/추가 locale catalog는 39개다`처럼 구분하세요.
- **근거**: `ui/src/i18n/locales/en.json:1`와 `ui/src/i18n/locales/ko.json:1`이 모두 존재합니다. 실제 파일 목록은 `ar`부터 `zh-TW`까지 40개 JSON입니다.

### 9. workspace diff viewer LOC가 현재 코드와 다름

- **위치**: line 59
- **현재 본문 인용**: ``packages/plugins/plugin-workspace-diff/src/ui/index.tsx` (854 LOC)``
- **문제 카테고리**: 정합성
- **제안 수정안**: `854 LOC`를 `824 LOC`로 갱신하세요.
- **근거**: `packages/plugins/plugin-workspace-diff/src/ui/index.tsx:824`가 파일 마지막 라인입니다.

### 10. `api/`가 shared zod 스키마를 그대로 import해 검증한다는 설명은 과함

- **위치**: line 65, line 89
- **현재 본문 인용**: ``api/` 가 `packages/shared` 의 zod 스키마를 그대로 import 해 *타입 끝까지 안전*`
- **문제 카테고리**: 정합성
- **제안 수정안**: `ui/src/api/는 packages/shared 타입을 많이 공유하고, 일부 응답(auth 등)은 shared zod schema로 런타임 검증한다. 대부분 API 클라이언트는 TypeScript 타입 파라미터 기반이다.` 정도로 낮추세요.
- **근거**: `ui/src/api/issues.ts:1-25`는 shared 타입만 import하고 `api.get<Issue[]>`로 반환 타입을 지정합니다. 공통 fetch 래퍼는 `ui/src/api/client.ts:22-36`에서 JSON을 반환할 뿐 zod parse를 하지 않습니다. 반면 auth 쪽은 `ui/src/api/auth.ts:32-36`, `ui/src/api/auth.ts:125-129`에서 schema parse를 수행합니다.

### 11. WebSocket event payload 필드명이 snake_case로 잘못 적힘

- **위치**: line 93, line 111
- **현재 본문 인용**: `event.payload.entity_type === 'issue'`
- **문제 카테고리**: 정합성
- **제안 수정안**: 의사코드를 `event.payload.entityType === 'issue'`로 고치세요.
- **근거**: 실제 invalidation 코드는 `ui/src/context/LiveUpdatesProvider.tsx:650`에서 `payload.entityType`을 읽고, `ui/src/context/LiveUpdatesProvider.tsx:852-853`에서 `activity.logged`를 처리합니다. `LiveEvent` payload 자체는 `packages/shared/src/types/live.ts:3-8`의 `Record<string, unknown>`입니다.

### 12. `plugin.*` live event가 `LiveUpdatesProvider`에서 분기된다는 주석은 현재 구현과 다름

- **위치**: line 116
- **현재 본문 인용**: `// …나머지 LIVE_EVENT_TYPES (heartbeat.run.*, agent.status, plugin.*) 로 분기`
- **문제 카테고리**: 정합성
- **제안 수정안**: `heartbeat.run.queued/status`, `agent.status`, `activity.logged` 중심으로 고치거나, `plugin.*`는 live event type에는 있지만 이 provider의 화면 invalidation 분기에는 아직 연결되지 않았다고 명시하세요.
- **근거**: `packages/shared/src/constants.ts:596-606`에는 `plugin.ui.updated`, `plugin.worker.crashed`, `plugin.worker.restarted`가 타입으로 존재하지만, `ui/src/context/LiveUpdatesProvider.tsx:817-867`의 handler는 `heartbeat.run.queued/status`, `agent.status`, `activity.logged`만 실질 처리합니다.

### 13. shadcn/ui 컴포넌트 경로가 잘못됨

- **위치**: line 128
- **현재 본문 인용**: `Paperclip 의 `ui/components/` 는 이 모델을 따른다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `Paperclip 의 `ui/src/components/` 는 이 모델을 따른다.`로 고치세요.
- **근거**: `ui/src/App.tsx:2`도 `@/components/ui/button`을 import하며, 실제 shadcn-style primitive들은 `ui/src/components/ui/` 아래에 있습니다.

### 14. shadcn/ui의 장점 설명 중 "의존성 잠금에서 자유롭다"는 과장됨

- **위치**: line 130
- **현재 본문 인용**: `의존성 잠금에서 자유롭다 — 라이브러리 마이그레이션이 없다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `shadcn 컴포넌트 자체의 패키지 버전 락인은 줄지만, Radix/Tailwind/유틸 의존성은 여전히 package.json에서 관리된다.`로 완화하세요.
- **근거**: `ui/package.json:49`에 `@radix-ui/react-slot`, `ui/package.json:60`에 `radix-ui`, `ui/package.json:78`에 `tailwindcss` 의존성이 있습니다.

### 15. `brand_color`가 "회사 생성 UI"에도 연결된다는 표현은 현재 UI와 다름

- **위치**: line 130
- **현재 본문 인용**: `회사 선택/생성 UI의 색상 스와치에 연결한다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `회사 아이콘, 회사 설정의 brand color 입력, 일부 회사 선택 UI의 색상 표시`로 고치세요.
- **근거**: `packages/db/src/schema/companies.ts:28`에 `brandColor` 컬럼이 있고, `ui/src/components/CompanyPatternIcon.tsx:105`와 `ui/src/components/CompanyPatternIcon.tsx:177-179`에서 패턴 hue에 사용됩니다. 회사 설정 화면은 `ui/src/pages/CompanySettings.tsx:338-371`에서 color input을 제공합니다. 반면 onboarding 회사 생성 step은 `ui/src/components/OnboardingWizard.tsx:107-110`에 `companyName`, `companyGoal`만 있고 brand color 입력 상태가 없습니다.

### 16. SPEC 절 번호가 틀림

- **위치**: line 159
- **현재 본문 인용**: `그래서 SPEC §10의 V1 must-have 안에 Web UI가 명시되어 있다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `SPEC-implementation §5.1 In Scope`로 고치세요.
- **근거**: `doc/SPEC-implementation.md:60-71`의 In Scope 목록에 `Board web UI for dashboard, org chart, tasks, agents, approvals, costs`가 있습니다. `doc/SPEC-implementation.md:544`부터의 §10은 REST API Contract입니다.

## 표현

### 1. "폴리시됐다"는 의미가 불명확하고 번역투에 가까움

- **위치**: line 55, line 57
- **현재 본문 인용**: `다수 컴포넌트가 모바일 viewport 흐름과 PWA 설치 모드... 를 보강했다.`
- **문제 카테고리**: 표현
- **제안 수정안**: line 55는 이미 `보강했다`가 자연스럽습니다. line 57의 `함께 폴리시됐다`는 `함께 다듬어졌다` 또는 `함께 보강됐다`로 바꾸는 편이 좋습니다.
- **근거**: 의미 변화 없이 격식/직역 흔적만 줄이는 수정입니다.

## 품질

### 1. `ui/src/` 디렉터리 구조 예시에 `i18n/`이 빠져 있음

- **위치**: line 67, 코드 1
- **현재 본문 인용**: `**코드 1. `ui/src/` 디렉터리 구조**`
- **문제 카테고리**: 품질
- **제안 수정안**: 코드 1에 `├── i18n/                 # i18next wrapper + locale catalog`를 추가하세요. 특히 §2.1에서 i18n locale catalog를 주요 변화로 다루므로 트리에도 반영하는 편이 흐름이 맞습니다.
- **근거**: `ui/src/i18n/locales/en.json:1` 및 `ui/src/i18n/locales/ko.json:1`이 존재합니다.

### 2. 그림/표 캡션 vs 본문 참조

이상 없음.
