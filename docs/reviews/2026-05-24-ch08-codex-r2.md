# ch08 Round 2 리뷰 — 08-usage.md

## 정합성

### F1

- **위치**: line 73
- **현재 본문 인용**: `첫 화면은 이 두 입력으로 끝난다(...). ... **월 예산은** 별도의 예산 API(\`PATCH /api/companies/:companyId/budgets\`, \`server/src/routes/costs.ts:280\`)와 비용 화면(\`/costs\`)에서 조정한다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `server/src/routes/costs.ts:280` 뒤에 `server/src/app.ts:196-224,314-317`를 추가하거나, endpoint 표기는 `PATCH /api/companies/:companyId/budgets`이고 route-local 정의는 `server/src/routes/costs.ts:280`임을 분리해 쓰기.
- **근거**: `server/src/routes/costs.ts:280`의 실제 router path는 `"/companies/:companyId/budgets"`로 `/api` prefix가 없다. `/api` prefix는 `costRoutes`가 API router에 붙고(`server/src/app.ts:196-224`), 그 API router가 `app.use("/api", api)`로 mount될 때 생긴다(`server/src/app.ts:314-317`).

### F2

- **위치**: line 167
- **현재 본문 인용**: `위 compose 는 \`db\`(postgres:17-alpine) 와 \`server\`(Paperclip Dockerfile) 두 서비스를 함께 띄우며 \`BETTER_AUTH_SECRET\` 이 강제된다(\`docker/docker-compose.yml:18-31\`, \`doc/DOCKER.md:84-89\`).`
- **문제 카테고리**: 정합성
- **제안 수정안**: compose 근거를 `docker/docker-compose.yml:2-16,18-36`으로 수정.
- **근거**: `db` 서비스와 `postgres:17-alpine`은 `docker/docker-compose.yml:2-16`에 있고, `server` 및 `BETTER_AUTH_SECRET` 강제는 `:18-31`에 있다. 두 서비스의 healthcheck 의존 연결은 `depends_on`의 `:34-36`에 있다. 현 인용 `:18-31`만으로는 `db` 서비스와 "두 서비스를 함께" 띄운다는 부분을 직접 검증할 수 없다.

### F3

- **위치**: line 198
- **현재 본문 인용**: `**\`/dashboard/live\`** 는 \`/api/companies/:companyId/live-runs\` 조회와 run log polling 으로 *활성/최근 에이전트 run* 을 보여 준다 (\`ui/src/components/ActiveAgentsPanel.tsx:78-81\`).`
- **문제 카테고리**: 정합성
- **제안 수정안**: 인용을 `ui/src/components/ActiveAgentsPanel.tsx:78-81,109-116`, `ui/src/api/heartbeats.ts:108-120`, `ui/src/components/transcript/useLiveRunTranscripts.ts:250-259`로 보강.
- **근거**: `ActiveAgentsPanel.tsx:78-81`은 `heartbeatsApi.liveRunsForCompany()` 호출만 보여 준다. 실제 `/companies/:companyId/live-runs` API path는 `ui/src/api/heartbeats.ts:108-120`에 있고, run log polling은 `ActiveAgentsPanel`이 `useLiveRunTranscripts()`에 polling 옵션을 넘기는 부분(`ui/src/components/ActiveAgentsPanel.tsx:109-116`)과 hook 내부 interval 처리(`ui/src/components/transcript/useLiveRunTranscripts.ts:250-259`)에서 확인된다.

## 표현

이상 없음

## 품질

이상 없음
