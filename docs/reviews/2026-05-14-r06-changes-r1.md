# R06 라운드 1 변경 요약

대상: `docs/research/06-drizzle-pglite.md`. ACCEPT 3/3 반영.

### 1. L11 drizzle-kit 명령 4 + studio 분리
- AS-IS: generate/migrate/push/studio (4개 명령)
- TO-BE: generate/migrate/push/pull (4 핵심) + 별도 studio UI

### 2. L22 표 — 마이그레이션 행
- AS-IS: `drizzle-kit generate/migrate/push/studio`
- TO-BE: `drizzle-kit generate/migrate/push/pull` + 별도 `studio` UI

### 3. L40 §4 paperclip 연결 — 테이블 수 + 마이그레이션 결정 경로 분리
- AS-IS: "75 개 안팎" / `drizzle.config.ts` 한 줄로 마이그레이션 대상 결정
- TO-BE: "86 개 pgTable() 정의를 78 개 스키마 파일" / `drizzle.config.ts` 는 Drizzle Kit 용도, 실제 `db:migrate` 는 `resolveMigrationConnection()` 의 fallback 체인 (`DATABASE_URL` → `.paperclip` → embedded-postgres) 사용 + `migration-runtime.ts:183-193` 인용

## 변경된 파일
- `docs/research/06-drizzle-pglite.md` (3 라인 수정)
