# 챕터 00 — 라운드 1 변경 요약

## 적용된 수정 (8개 ACCEPT 반영 + 인라인 citation)

### 1) §2 문제 목록: 5 → 6 (A1)
- `:17` 인용을 `README.md:131-140` 로 축소 (GOAL.md 제거 — enumeration 없음)
- 6번째 항목 "아이디어→repo→Claude Code→babysit" 추가

### 2) §3 그림 1 본문: 어댑터 카운트 11 → 12 (A2)
- "11개 / 9 runtime + 2 generic / Hermes 포함" → "12개 / 10 runtime + 2 generic"
- 런타임 목록에 Cursor Cloud 신규 명시, ACP → ACP-x 정정
- `server/src/adapters/registry.ts:480-493` 인라인 인용 추가

### 3) §3 어댑터 계약: SPEC vs 구현 분리 (A3)
- `:37` "최소 계약은 `invoke / status / cancel` 3개 메서드뿐" → SPEC(`doc/SPEC.md:207-215`) vs 구현(`packages/adapter-utils/src/types.ts:349-352`) 두 줄로 분리. 35줄의 본문 그림 설명("execute / testEnvironment / …")과 일관성 회복.

### 4) §3 그림 1-A 본문 참조 굵음 제거 (A8)
- `:39` "`**그림 1-A**가 그 원형이다`" → "그림 1-A가 그 원형이다"
- 캡션은 그대로 유지(외부 인용 보조 번호 체계 의도적)

### 5) §4 표 1 서두 문장 재작성 (A5)
- `:47` "PRODUCT와 SPEC이 일관되게 강조하는 다섯 원칙" 표현 제거
- "PRODUCT.md 다섯 제품 원칙(`doc/PRODUCT.md:61-71`) + SPEC.md §13 아홉 consolidated principles(`doc/SPEC.md:521-531`)를 학습용으로 다섯 축으로 추리면" 으로 정정. 표 1 표 자체는 보존.

### 6) §4 코드 매핑 단락: 원칙 4 톤 보정 (A4)
- `:59` `issues.parent_id` self-FK → `packages/db/src/schema/issues.ts:28-29` 인용 + "`goal_id`/`parent_id` 모두 nullable, fallback 서비스가 채움" 명시
- 단락 끝에 "원칙은 슬로건이고 구현은 fallback이 있는 nullable" 주의 한 문장 추가

### 7) §5 V1 Out 해설 단락: Plugin 영역 단정 완화 (A6)
- `:72` "Out 열에 들어간 항목은 곧 *플러그인이 메우는 영역*" → KB·매출 회계·ClipHub만 future plugin(`doc/SPEC.md:515-517`)이고, multi-board governance·self-healing은 거버넌스/오케스트레이션 확장이라고 분리

### 8) §5 V1 출처 line citation 추가 (A9)
- `:63` `doc/SPEC-implementation.md` → `doc/SPEC-implementation.md:60-82`

### 9) §6 페르소나 5 → 7 (A7)
- `:76-82` 5개 페르소나 + 비용 모니터링 + 태스크 매니저 흐름 2개 추가
- `README.md:70-78` 인라인 인용

## REJECT 1건의 사유

**R1. CJK bold 뒤 공백 광범위 적용** — `CLAUDE.md` 규칙은 **`**...**` 내부에 구두점이 포함된 경우**에만 닫는 `**` 뒤 공백을 요구한다. Codex가 든 `**Paperclip**은`, `**control plane**이다`, `**그림 1-A**가` 등은 내부에 구두점이 없거나 `-` 뿐이라 CommonMark right-flanking 규칙상 깨지지 않는다. 일괄 공백 삽입은 가독성만 해치므로 적용하지 않음.

## 변경 정량

- 챕터 00 LOC: 변경 전 85줄 → 변경 후 87줄 (+2). 실질 수정 라인은 \~20줄(추가/대체).
- 누적 변경 LOC (11챕터 중 1챕터): \~20 / 200 checkpoint
- Scope: `00-overview.md` 만, 코드 변경 0건.

## 다음 라운드 요청

이 변경에 대해 라운드 2 검토를 요청한다. 본 변경 외에 새로 발견한 사실 오류, 표현 효율성·한국어 자연성·마크다운 관습 위반이 있다면 같은 형식으로 재지적해 달라. 더 이상 ACCEPT 가능한 항목이 없다면 `===CODEX_FINAL_APPROVAL===` 한 줄로 응답.
