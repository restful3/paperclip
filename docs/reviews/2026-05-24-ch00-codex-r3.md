# ch00 Round 3 — Codex Review

## 지적 사항

### 1. `ACP-x` 표기 불일치

- **위치**: line 30
- **현재 본문 인용**: `11개는 구체 런타임(Claude Code, Codex, Cursor local, Cursor Cloud, Gemini, Grok local, OpenCode, Pi, ACP-x, OpenClaw 게이트웨이, Hermes)을 향하고`
- **문제 카테고리**: 품질
- **제안 수정안**: `ACP-x`를 `ACPX`로 수정. 예: `..., OpenCode, Pi, ACPX, OpenClaw 게이트웨이, Hermes)`
- **근거**: 실제 adapter type은 `acpx_local`이고, adapter label은 `ACPX (local)`이다. `server/src/adapters/registry.ts:268-270`에서 `type: "acpx_local"`로 등록되며, `packages/adapters/acpx-local/src/index.ts:3-4`도 `type = "acpx_local"`, `label = "ACPX (local)"`로 정의한다. 같은 리포트의 ch04도 `acpx-local` / `Agent Client Protocol bridge`로 표기한다.

## 재검증 결과

- line 13의 `README.md:31` 슬로건 인용 추가는 정확하다.
- line 30의 built-in adapter type 13개 및 `server/src/adapters/registry.ts:511-524` 인용은 현재 코드와 일치한다.
- line 60의 `88개 테이블 중 67개` 카운트는 현재 `packages/db/src/schema/*.ts` 기준 재집계 결과와 일치한다.
- line 60의 `doc/execution-semantics.md` 인용 범위 `:155-172`, `:174-197`, `:207-240`은 현재 문서 구조와 일치한다.
