# ch02 Round 1 — Changes Applied

_2026-05-24, codex-a / Round 1 → 2 송부용_

## docs/report/02-data-model.md — 9곳 수정

```diff
- 86개의 `pgTable()` 정의가 78개 스키마 파일 ... (`index.ts:1-78`)
+ 88개의 `pgTable()` 정의가 79개 `pgTable` 보유 스키마 파일 ... (`index.ts:1-79`)

- **모든 비즈니스 데이터가 `company_id`로 스코프**
+ **핵심 회사 도메인 데이터가 `company_id`로 스코프**

- 80여 개 테이블 중 60여 개가 `company_id uuid not null references companies.id` 컬럼을 들고 있다.
+ 88개 `pgTable()` 정의 중 67개가 `company_id` 컬럼을 들고 있다. 다만 일부 플러그인/인스턴스/파생 테이블은 `plugin_id`, `user_id`, 상위 company-scoped FK 등 다른 축으로 귀속된다.

- `requested_by_agent_id?` ⊕ `requested_by_user_id?`
+ `requested_by_agent_id?`, `requested_by_user_id?`

- 핵심은 `doc/SPEC.md` §8의 의사코드 한 줄 — *"affected_rows == 1 일 때만 성공"* — 이지만
+ 핵심은 `doc/SPEC-implementation.md` §10.4.1의 atomic checkout contract — 단일 SQL update를 시도하고, updated row count가 0이면 `409`를 반환한다는 규칙 — 이지만

- `cloud_upstreams` (#6548 신규 — 마이그레이션 `0089`, local → cloud 회사 동기화)
+ `cloud_upstream_connections`, `cloud_upstream_runs` (#6548 신규 — 마이그레이션 `0089`, local → cloud 회사 동기화)

- `users` (Better Auth), `instance_*`, `auth.*`
+ Better Auth 테이블(`user`, `session`, `account`, `verification`), `instance_*`

- 표 2의 군집은 `server/src/services/`의 디렉터리 구조와도 거의 1:1로 대응된다.
+ 표 2의 군집은 `server/src/services/`의 파일명·서비스 경계와 대체로 대응된다.

- 이 패키지는 `package.json` 에서 `embedded-postgres@18.1.0-beta.16` 으로 핀(pin)되어 있고, 한 줄짜리 패치(`patches/embedded-postgres@*.patch`) 가 적용된다. PostgreSQL 17 (full native) 을 그대로 호스트하므로 PGlite (WASM) 보다 호환성이 좋다.
+ 이 패키지는 `packages/db/package.json` 에서 `embedded-postgres@^18.1.0-beta.16` 으로 선언되고, 루트 `pnpm-lock.yaml` 과 `patches/embedded-postgres@*.patch` 에서 `18.1.0-beta.16` 으로 고정된다. WASM 이 아니라 플랫폼별 native PostgreSQL 바이너리를 띄우므로 PGlite (WASM) 보다 호환성이 좋다.

- `cost_events` 의 컬럼 구성이 의미심장하다 — `cached_input_tokens` 가 별도로 존재한다.
+ `cost_events` 의 컬럼 구성에서 중요한 점은 `cached_input_tokens` 가 별도 컬럼으로 존재한다는 것이다.
```

## DEFER

- F7 (`docs/research/06-drizzle-pglite.md:40` 의 86/78 → 88/79) — research scope 밖

## 누적 LOC delta (그룹 A)

- ch00: 7 LOC
- ch01: ~14 LOC (3 rounds)
- ch02 R1: ~15 LOC (9 Edit)
- **그룹 A 누적: ~36 LOC** (LOC_CHECKPOINT 200 까지 164 LOC 여유)
