# ch00 Round 4 — Codex Review

## 지적 사항

### 1. built-in adapter 등록 배열 인용 범위가 마지막 항목을 빠뜨림

- **위치**: line 30
- **현재 본문 인용**: `현재 upstream \`master\` 기준 13개의 built-in adapter type(\`server/src/adapters/registry.ts:511-524\`)`
- **문제 카테고리**: 정합성
- **제안 수정안**: `server/src/adapters/registry.ts:511-525` 또는 더 좁게 `server/src/adapters/registry.ts:513-525`
- **근거**: 현재 `server/src/adapters/registry.ts`에서 등록 배열은 `:513-525`에 13개 항목으로 존재한다. `:524`는 `processAdapter`이고, 마지막 `httpAdapter`는 `:525`다. 따라서 `:511-524`는 본문의 “13개” 근거 범위로는 한 줄 부족하다.

### 2. SPEC.md future plugin 인용 라인 오프셋

- **위치**: line 73
- **현재 본문 인용**: `KB는 future plugin으로 명시되어 있고(\`doc/SPEC.md:491\`, \`:500-502\`), 외부 매출/비용 회계도 future plugin이다(\`doc/SPEC.md:493\`, \`:517\`).`
- **문제 카테고리**: 정합성
- **제안 수정안**: `KB는 future plugin으로 명시되어 있고(\`doc/SPEC.md:493\`, \`:500-502\`), 외부 매출/비용 회계도 future plugin이다(\`doc/SPEC.md:495\`, \`:519\`).`
- **근거**: 현재 `doc/SPEC.md:493`이 `Knowledge base - a future plugin`이고, `:500-502`도 KB anti-goal/plugin 설명이다. 외부 revenue/expense는 `doc/SPEC.md:495`의 `Revenue/expense tracking beyond token costs - a future plugin`과 `:519`의 `Does not track external revenue/expenses — that's a future plugin`에 있다. 기존 `:491`은 `Not V1` 헤더이고, 기존 `:517`은 delivery infrastructure anti-requirement라 revenue/expense 근거가 아니다.

## 재검증 결과

- line 13의 `README.md:31` 슬로건 인용은 정확하다.
- line 30의 `ACPX` 표기 통일은 `packages/adapters/acpx-local/src/index.ts:3-4` 및 `server/src/adapters/registry.ts:269`와 일치한다.
- line 60의 `88개 테이블 중 67개` 카운트는 현재 `packages/db/src/schema/*.ts` 기준 재집계 결과와 일치한다.
- line 60의 `doc/execution-semantics.md` 인용 범위 `:155-172`, `:174-197`, `:207-240`은 현재 문서 구조와 일치한다.
