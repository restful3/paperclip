# 챕터 02 — 라운드 1 변경 요약

## 적용 (9 ACCEPT)

| # | 위치 | 변경 요약 |
|---|---|---|
| A1 | `:11` | "74개 파일 / 80여 개 정의" → "86개 `pgTable()` / 78 스키마 파일" + `schema/index.ts:1-78` 인용 |
| A2 | `:17` | "issues와 heartbeat_runs 사이에 직접 FK 없다" 정정 → `checkout_run_id`/`execution_run_id` 직접 참조 + activity_log.run_id는 보조 |
| A3 | `:17, 32` | cost_events `agent_id` 필수, 나머지 4개 nullable로 정정 + 표 1 행 갱신 + 인용 |
| A4 | (svg) | OUT-OF-SCOPE 격하 — 본문 텍스트에 svg 라벨 직접 인용 없음 |
| A5 | `:52, 60` | atomic checkout 라인 `3409\~3525` → `3593-3736`, status 예시 `open` → `backlog/todo/blocked/in_review` |
| A6 | `:93, 108` | 마이그레이션 위치 `packages/db/drizzle/` → `packages/db/src/migrations/` + `drizzle.config.ts:5` 인용. "자동 적용 없다" → dev/server boot 자동 적용 조건 명시 |
| A7 | `:132-134` | Supabase 예시를 direct(5432)와 pooled runtime(6543) + `DATABASE_MIGRATION_URL` 5432 두 줄로 분리 |
| A8 | `:87` | 표 2 Plugins 행 `plugin_database` → `plugin_database_namespaces`, `plugin_migrations` |
| A9 | `:110, 120` | `그림 4-A` → `그림 5`, `그림 4-B` → `그림 6`. 외부 출처 URL을 클릭 가능 링크로 |

## 변경 정량
- Ch02 LOC: 139 → 141 (실질 수정 \~30줄)
- 누적: ~102 / 200 checkpoint

## DEFER 2 / OUT-OF-SCOPE 2 — 적용 안 함 (Codex r1 결정 + A4 격하)

## 다음
라운드 2 재리뷰.
