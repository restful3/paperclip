# Ch08 — 라운드 1 변경 요약

## 적용한 ACCEPT (6건)

| 항목 | 수정 위치 | 변경 골자 |
|---|---|---|
| 8-4 §4 회사 만들기 | line 69-75 | "이름, 설명, brand_color / 월 예산 / issue prefix / 첫 골" 4 항목 → 실제 첫 화면 입력 2 항목(`Company name` + `Mission/goal`). 나머지는 회사 설정에서 채운다는 주석 + `issue_prefix` 서버 파생 인용 추가 |
| 8-6 §5 첫 에이전트 표 | line 87-100 | 표에서 `icon` / `capabilities` / `budget_monthly_cents` 행 제거. `role / title` 행을 `title` · `role` · `reports_to` · `desiredSkills` 로 재구성. 표 아래 노트로 "생성 화면은 이 항목만, 나머지는 edit 화면 / payload는 `budgetMonthlyCents: 0` 고정" 추가 |
| 8-8 §5 그림 8 참조 | line 99 | `**그림 8**은` → `그림 8은` (본문 그림 참조 평문화) |
| 8-11 §8 Docker 코드 5 | line 152-163 | "compose가 PostgreSQL 만 띄움" → "compose 가 PostgreSQL 17 + Paperclip server 풀스택을 함께 띄움". 실제 명령은 `BETTER_AUTH_SECRET=$(...) docker compose -f docker/docker-compose.yml up --build`. 진실 소스 인용 추가 |
| 8-12 §9 호스티드 코드 6 | line 165-180 | "환경 변수 3개" → "핵심 환경 변수 묶음". `BETTER_AUTH_SECRET`, `PAPERCLIP_PUBLIC_URL` 추가. `authenticated/public` 모드 필수 조건 인용 추가 |
| 8-13 §10 dashboard/live | line 181 | "라이브 활동 피드" → "활성/최근 에이전트 run", `/activity` 와 별도 화면임을 1줄로 명시 |

## 변경 LOC 누적 (대략)

- 추가: ~14 줄
- 삭제: ~7 줄
- 순 증가: ~7 줄 (Ch08 본문 184 → 약 195 줄로)

## REJECT — 없음

## DEFER 보존

- 라인 범위 ±1~2 nit, 영문 용어 취향, 코드 캡션 문체

## 다음

`docs/reviews/2026-05-14-ch08-codex-r2.md` 로 재리뷰 요청.
