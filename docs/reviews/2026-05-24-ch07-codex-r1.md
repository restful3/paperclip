# ch07 정합성/표현 리뷰 — Codex r1

대상: `docs/report/07-governance-cost.md`

## 정합성

### 1. Single human Board의 SPEC 절 번호가 틀림

- **위치**: line 16
- **현재 본문 인용**: `V1의 보드는 *단일* 인간 운영자다(SPEC §10의 *Single human Board.*).`
- **문제 카테고리**: 정합성
- **제안 수정안**: `V1의 보드는 *단일* 인간 운영자다(\`SPEC-implementation.md:35\`의 "Single human board operator per deployment"; \`SPEC.md:24\`의 "V1: Single human Board.").` 처럼 절 번호 대신 실제 파일/라인을 쓰는 편이 안전합니다.
- **근거**: `doc/SPEC-implementation.md:35`에 `Board | Single human board operator per deployment`가 있고, `doc/SPEC.md:24`에 `V1: Single human Board. One human operator.`가 있습니다. `doc/SPEC-implementation.md:544`부터의 §10은 `API Contract (REST)`입니다.

### 2. approval constants 라인 인용이 stale

- **위치**: line 38
- **현재 본문 인용**: ``packages/shared/src/constants.ts:373\~388` 가 단일 소스다`
- **문제 카테고리**: 정합성
- **제안 수정안**: ``packages/shared/src/constants.ts:408-422` 가 단일 소스다`로 고치세요.
- **근거**: `packages/shared/src/constants.ts:373-388`은 routine trigger/run constants이고, 실제 `APPROVAL_TYPES`는 `packages/shared/src/constants.ts:408-413`, `APPROVAL_STATUSES`는 `packages/shared/src/constants.ts:416-422`에 있습니다.

### 3. 신규 에이전트 approval activation 인용 범위가 너무 좁음

- **위치**: line 42
- **현재 본문 인용**: ``server/src/services/agents.ts:534-537``
- **문제 카테고리**: 정합성
- **제안 수정안**: `server/src/services/approvals.ts:102-117`와 `server/src/services/agents.ts:534-548`를 함께 인용하세요. activation이 `hire_agent` approval 처리에서 호출되고, `pending_approval` 조건과 반환 처리는 538-548까지 이어집니다.
- **근거**: `server/src/services/approvals.ts:112-117`에서 `hire_agent` 승인 시 payload의 `agentId`로 `agentsSvc.activatePendingApproval()`를 호출합니다. `server/src/services/agents.ts:534-548`에서 `agents.status='pending_approval'`인 row만 `idle`로 갱신하고, 갱신 여부를 반환합니다.

### 4. CEO 초기 전략 approval은 현재 코드 구현 근거가 없음

- **위치**: line 43
- **현재 본문 인용**: `CEO가 첫 heartbeat에서 작성한 전략 트리(이슈 분해)를 보드가 \`approvals.type='approve_ceo_strategy'\`로 승인해야 다음 heartbeat가 돌기 시작한다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: 현재 구현 설명이라면 `approve_ceo_strategy` 흐름을 제거하거나 `SPEC상 V1 목표로 남아 있으나 현재 코드에서는 approval type 상수만 존재한다`로 낮추세요.
- **근거**: `packages/shared/src/constants.ts:408-413`에 `approve_ceo_strategy` 값은 있지만, `rg "approve_ceo_strategy|ceo_strategy"` 기준 서버/패키지 코드에서 실제 생성·승인·heartbeat gate 구현은 확인되지 않습니다. 반면 SPEC 목표는 `doc/SPEC-implementation.md:67`, `doc/SPEC-implementation.md:768-772`에 남아 있습니다.

### 5. 결정 override 라우트가 실제 라우트와 다름

- **위치**: line 60
- **현재 본문 인용**: ``결정 override | `issue_execution_decisions` 에 board 결정 기록 | `POST /api/issues/:id/decisions` ``
- **문제 카테고리**: 정합성
- **제안 수정안**: 라우트를 `PATCH /api/issues/:id`로 고치고, `execution policy transition이 decision을 만들 때 issue_execution_decisions에 기록`된다고 설명하세요. watchdog decision까지 포함하려면 별도 행으로 `POST /api/heartbeat-runs/:runId/watchdog-decisions`를 분리해야 합니다.
- **근거**: `server/src/routes/issues.ts:3394`가 `PATCH /issues/:id` route이고, 그 내부에서 `transition.decision`이 있을 때 `issue_execution_decisions`를 insert합니다(`server/src/routes/issues.ts:3580-3609`, `server/src/routes/issues.ts:3670-3695`). `POST /issues/:id/decisions` route는 검색되지 않습니다. watchdog decision route는 `server/src/routes/agents.ts:3221-3255`의 `POST /heartbeat-runs/:runId/watchdog-decisions`입니다.

### 6. 비용 normalization 인용 라인이 실제 비용 정규화 코드가 아님

- **위치**: line 83
- **현재 본문 인용**: ``server/src/services/heartbeat.ts:1299-1302`, `packages/shared/src/validators/cost.ts:4-18``
- **문제 카테고리**: 정합성
- **제안 수정안**: adapter run 경로는 `costUsd -> costCents` 변환 근거로 `server/src/services/heartbeat.ts:1358-1361`, `server/src/services/heartbeat.ts:6764-6803`을 인용하세요. API 보고 경로는 `costCents`만 받는다고 분리해 `packages/shared/src/validators/cost.ts:4-23`, `server/src/routes/costs.ts:73-85`를 인용하세요.
- **근거**: `server/src/services/heartbeat.ts:1299-1302`는 run failure summary 텍스트 처리 부근이라 cost ledger 정규화가 아닙니다. 실제 adapter 결과의 달러 비용은 `normalizeBilledCostCents()`에서 cents로 변환되고(`server/src/services/heartbeat.ts:1358-1361`), `costs.createEvent()`에 `costCents`로 저장됩니다(`server/src/services/heartbeat.ts:6764-6803`). 반면 cost event API validator는 `costCents`를 필수로 받고 `costUsd` 필드는 없습니다(`packages/shared/src/validators/cost.ts:4-23`).

### 7. `budget_incidents`의 "실제 컬럼" 목록이 누락됨

- **위치**: line 83
- **현재 본문 인용**: ``budget_incidents` 의 실제 컬럼은 `threshold_type ('soft'|'hard')`, `metric`, `window_kind/start/end`, `amount_limit`, `amount_observed`, `status`, `approval_id?`, `resolved_at?` 이다`
- **문제 카테고리**: 정합성
- **제안 수정안**: "핵심 컬럼"이라고 낮추거나, 실제 컬럼 목록에 `id`, `company_id`, `policy_id`, `scope_type`, `scope_id`, `created_at`, `updated_at`도 포함하세요.
- **근거**: `packages/db/src/schema/budget_incidents.ts:10-26`에 `companyId`, `policyId`, `scopeType`, `scopeId`, timestamp 컬럼이 함께 정의되어 있습니다. hard incident 생성도 이 값들을 채워 insert합니다(`server/src/services/budgets.ts:394-410`).

### 8. hard cap 시퀀스의 코드 라인 범위가 active run 취소를 포함하지 않음

- **위치**: line 93, line 95
- **현재 본문 인용**: `(services/budgets.ts:213-253)` / `pending wakeup을 일괄 취소 (services/heartbeat.ts:9431-9456)`
- **문제 카테고리**: 정합성
- **제안 수정안**: ②는 `server/src/services/budgets.ts:213-258`로, ③은 `server/src/services/heartbeat.ts:9579-9604` 및 helper `server/src/services/heartbeat.ts:9431-9477`로 갱신하세요.
- **근거**: `pauseAndCancelScopeForBudget()`는 hook 호출까지 `server/src/services/budgets.ts:251-257`에 걸쳐 있습니다. active run 취소와 pending wakeup 취소를 묶는 함수는 `cancelBudgetScopeWork()`이며 `server/src/services/heartbeat.ts:9579-9604`에 있습니다. 현재 인용한 `server/src/services/heartbeat.ts:9431-9456`은 pending wakeup id 조회 일부만 포함하고, 실제 update/cancel 처리는 `9431-9477`까지 이어집니다.

### 9. `costs.byProject` 인용 범위가 query 본문 중간에서 끊김

- **위치**: line 113
- **현재 본문 인용**: ``costs.byProject` (`server/src/services/costs.ts:454\~492`)`
- **문제 카테고리**: 정합성
- **제안 수정안**: `server/src/services/costs.ts:454-502`로 갱신하세요. `docs/research/10-llm-cost-budgeting.md`에도 같은 stale 범위가 있으므로 함께 맞추는 편이 좋습니다.
- **근거**: `byProject`는 `server/src/services/costs.ts:454`에서 시작하고, `coalesce(cost_events.project_id, run_project_links.project_id)`와 join/group/order query가 `server/src/services/costs.ts:480-501`까지 이어진 뒤 `502`에서 닫힙니다.

### 10. activity_log cleanup 인용 범위가 실제 delete 라인과 다름

- **위치**: line 136
- **현재 본문 인용**: ``server/src/services/agents.ts:501-516`, `server/src/services/companies.ts:265-270``
- **문제 카테고리**: 정합성
- **제안 수정안**: `server/src/services/agents.ts:501-518`, `server/src/services/companies.ts:267-283`으로 고치세요.
- **근거**: agent 삭제의 `activityLog` delete는 `server/src/services/agents.ts:513-518`에서 닫힙니다. company 삭제의 `activityLog` delete는 `server/src/services/companies.ts:282`이고, 삭제 transaction은 `server/src/services/companies.ts:267`에서 시작해 다음 cleanup으로 이어집니다.

## 표현

이상 없음.

## 품질

이상 없음.
