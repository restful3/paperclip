# Codex 리뷰 — ch00 (00-overview.md) Round 1

_원본: codex-a 응답 / 2026-05-24 09:27 / 작업 시간 2m 53s_
_참고: Codex가 저장 지시를 무시해 화면 capture에서 발췌. 응답 절단 없음 확인._

---

## Findings

### 1. line 60 — 테이블/company scope 카운트 STALE
- **위치**: `docs/report/00-overview.md:60`
- **현재 본문 인용**: "80여 개 테이블 중 약 60개"
- **카테고리**: 정합성
- **근거**: 현재 `packages/db/src/schema/*.ts` 기준 `pgTable`은 **88개**, `company_id` 컬럼 가진 테이블은 **67개**. `docs/report/CHANGE-IMPACT-2026-05-23.md:58` 도 88개 기준. Codex 자체 검증: Perl 스크립트로 `tables=88 company_id_tables=67` 확인.
- **제안 수정**: "88개 테이블 중 67개" 또는 느슨하게 "약 70개"

### 2. line 60 — `doc/execution-semantics.md:105` 인용 근거 미스매치
- **위치**: `docs/report/00-overview.md:60`
- **카테고리**: 정합성
- **근거**: 실제 `doc/execution-semantics.md:105` 는 "user-owned issues에는 stranded-work reconciliation이 적용되지 않는다"는 **예외 설명**. "회복 규칙" 근거로 인용하기엔 부적절.
- **제안 수정**: 회복 규칙 근거로는 다음 중 하나를 사용:
  - `:155-172` liveness contract
  - `:174-197` explicit recovery actions
  - `:207-240` stalled 신호

### 3. line 30 — ch00 의 어댑터 카운트는 정확. 단, ch04 와 정합성 깨짐
- **위치**: `docs/report/00-overview.md:30` (참고 정보, ch00 자체는 OK)
- **카테고리**: 정합성 (cross-chapter)
- **근거**: `registerBuiltInAdapters()` 가 `server/src/adapters/registry.ts:511` 에서 **13개** 등록 — ch00 본문과 일치. 그러나 `docs/report/04-adapters-and-skills.md:25` 는 아직 "built-in 10종 + 범용 2종"이고 표에도 grok-local 누락.
- **제안 수정**: ch00 본문 변경 ❌. **ch04 가 후속 수정 대상** — 그룹 A 의 ch04 라운드에서 처리.

### 4. line 13 — 슬로건 인용 위치 누락
- **위치**: `docs/report/00-overview.md:13`
- **카테고리**: 품질 (부가 권장)
- **현재 본문 인용**: 슬로건 문장 (정확함)
- **근거**: 슬로건 원문은 `README.md:31` 에 있으며, 다른 문장들이 인용 위치(`파일:라인`)를 붙이는 스타일이라면 일관성 위해 추가 권장.
- **제안 수정**: 슬로건 끝에 ` (README.md:31)` 부착 (선택적).

---

## 검증 메모 (Codex가 검증 후 OK 판정)

다음 인용들은 대조 결과 정상:

- README 문제 표 `README.md:131-140`
- SPEC adapter contract `doc/SPEC.md:207-215`
- 현재 `ServerAdapterModule` 필수 메서드 `packages/adapter-utils/src/types.ts:349`
- V1 scope `doc/SPEC-implementation.md:60-82`

## 환경 메모

- 요청 메시지에 적힌 `docs/research/00-paperclip-overview.md` 는 **존재하지 않음**. 리서치 노트는 `docs/research/01-*` 부터 `10-*` 까지만 있음. (다음 라운드부터 참고 자료 안내 시 정정 필요)

---

## 분류 요약 (참고 — Claude 의 분류는 별도 decisions 파일)

| # | Finding | 즉시 ACCEPT 후보 | 비고 |
|---|---|---|---|
| 1 | 테이블 카운트 STALE | ✅ | Codex가 직접 카운트로 검증 |
| 2 | execution-semantics.md:105 인용 미스매치 | ✅ | 라인 재확인 필요 |
| 3 | ch04 후속 수정 (ch00 본문 변경 X) | DEFER → ch04 라운드 | scope 밖, 메모만 |
| 4 | 슬로건 인용 위치 추가 | DEFER (부가 권장) | 일관성 검토 후 결정 |
