# 챕터 07 리뷰 — 라운드 3

## ACCEPT 1

- 위치: `docs/report/07-governance-cost.md:93`
- 현재: hard cap 시퀀스의 마지막 단계를 “unblock requires Board action (raise limit / acknowledge / 연결된 approval 결정)”로 설명합니다.
- 문제: 현재 budget incident resolution action은 `raise_budget_and_resume`과 `keep_paused` 두 가지입니다. `raise_budget_and_resume`은 예산을 올리고 scope를 resume하며 open incident들을 `resolved`로 바꿉니다. 반면 `keep_paused`는 incident를 `dismissed`로 만들고 연결 approval을 `rejected`로 표시할 뿐, scope를 resume하지 않으므로 “unblock/acknowledge”로 묶으면 실제 동작을 오해하게 됩니다.
- 제안: “Board resolution은 `raise_budget_and_resume`(예산 상향 + resume + approval approved) 또는 `keep_paused`(incident dismissed + approval rejected, scope는 계속 paused)”처럼 두 action의 효과를 분리해서 쓰세요.
- 근거: `packages/shared/src/constants.ts:545`, `packages/shared/src/constants.ts:546`, `packages/shared/src/constants.ts:547`, `packages/shared/src/validators/budget.ts:23`, `server/src/services/budgets.ts:879`, `server/src/services/budgets.ts:911`, `server/src/services/budgets.ts:915`, `server/src/services/budgets.ts:921`, `server/src/services/budgets.ts:922`, `server/src/services/budgets.ts:926`, `server/src/services/budgets.ts:931`
