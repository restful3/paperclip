# ch06 Round 1 — Changes Applied

_2026-05-24, codex-b / Round 1 → 2 송부용_

## docs/report/06-ui-and-board.md — 20곳 수정

### 라우팅 표 헤더 + 표 내용 (line 19~44)

```diff
- ## 2. 70+ 페이지의 라우팅 표
+ ## 2. 라우팅 표 (Route 129개)

- `/company/settings/{environments,access,invites,secrets}` | ...Secrets.tsx (2,155 LOC)...
+ `/company/settings/{environments,members,access,cloud-upstream,invites,secrets}` *(access는 legacy)* | ...Secrets.tsx (2,444 LOC)...

- `IssueRecoveryActionCard.tsx`(537 LOC)
+ `IssueRecoveryActionCard.tsx`(543 LOC)

- `/inbox/{mine,recent,unread,all,requests}`
+ `/inbox/{mine,recent,unread,blocked,all,requests}`

- | 인스턴스 설정 | `/instance/settings/adapters`, ... |
+ | 인스턴스 설정(어댑터·플러그인 발췌) | `/instance/settings/adapters`, ... *(외 profile·general·access·heartbeats·experimental)* |
```

### 라우팅 표 후속 단락 + 2.1 섹션 (line 46~59)

```diff
- `Secrets.tsx`(2,155 LOC, secrets/vault ...
+ `Secrets.tsx`(2,444 LOC, secrets/vault ...

- UI 표면이 다섯 축에서 동시에 확장됐다.
+ UI 표면이 여섯 축에서 동시에 확장됐다.

- `ui/src/i18n/locales/` 에 **39개 언어 JSON** 이 들어왔다
+ `ui/src/i18n/locales/` 에 **40개 locale JSON (en 포함)** 이 들어왔다

- `Secrets.tsx` (2,155 LOC) 에 AWS Secrets Manager
+ `Secrets.tsx` (2,444 LOC) 에 AWS Secrets Manager

- 함께 폴리시됐다.
+ 함께 다듬어졌다.

- `plugin-workspace-diff/src/ui/index.tsx` (854 LOC)
+ `plugin-workspace-diff/src/ui/index.tsx` (824 LOC)
```

### 디렉터리 구조 + zod 표현 (line 65~89)

```diff
- `api/` 가 `packages/shared` 의 zod 스키마를 그대로 import 해 *타입 끝까지 안전* 의 모노레포 이득을 챙기는 점은 두 확장점이 공통이다.
+ `api/` 는 `packages/shared` 의 타입을 공유한다 — 일부 응답(auth 등)은 shared zod schema 로 런타임 검증하고 대부분의 API 클라이언트는 TypeScript 타입 파라미터 기반이다. 이는 두 확장점이 공통이다.

  (코드 1 트리에 추가)
+ ├── i18n/                # i18next wrapper + locale catalog (40개)

- `api/` 가 `packages/shared` 의 zod 검증을 그대로 import 해 type 안전성을 끝까지 유지한다
+ `api/` 가 `packages/shared` 의 타입을 공유하고 일부 응답은 zod 로 런타임 검증한다
```

### WebSocket 의사코드 (line 111, 116)

```diff
- event.payload.entity_type === 'issue'
+ event.payload.entityType === 'issue'

- // …나머지 LIVE_EVENT_TYPES (heartbeat.run.*, agent.status, plugin.*) 로 분기
+ // …나머지 LIVE_EVENT_TYPES (heartbeat.run.queued/status, agent.status, activity.logged) 로 분기
```

### shadcn/ui 절 (line 128, 130)

```diff
- Paperclip 의 `ui/components/` 는 이 모델을 따른다.
+ Paperclip 의 `ui/src/components/` 는 이 모델을 따른다.

- (1) 의존성 잠금에서 자유롭다 — 라이브러리 마이그레이션이 없다.
+ (1) shadcn 컴포넌트 자체의 패키지 락인은 줄지만, Radix/Tailwind/유틸 의존성은 여전히 `package.json` 에서 관리된다.

- 을 `CompanyPatternIcon` 의 패턴 색상과 회사 선택/생성 UI의 색상 스와치에 연결한다.
+ 을 `CompanyPatternIcon` 의 패턴 색상, 회사 설정의 brand color 입력, 일부 회사 선택 UI 의 색상 표시에 연결한다.
```

### SPEC 인용 (line 159)

```diff
- 그래서 SPEC §10의 V1 must-have 안에 Web UI가 명시되어 있다.
+ 그래서 `doc/SPEC-implementation.md:60-71` §5.1 In Scope 의 V1 must-have 안에 Web UI 가 명시되어 있다.
```

## 누적 LOC delta (그룹 B)

- Round 1: ~22 LOC (-/+ pairs, 20곳 + 1 표현 + 1 품질 (i18n/ 추가))
- **누적: ~22 LOC** (LOC_CHECKPOINT 200 까지 178 LOC 여유)
