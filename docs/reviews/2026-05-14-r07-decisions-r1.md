# R07 라운드 1 결정

- ACCEPT 4 / REJECT 0 / DEFER 0

### 1. L87 워크스페이스 글롭 누락
- 코드 (`pnpm-workspace.yaml:1-14`): `packages/*`, `packages/adapters/*`, `packages/plugins/*`, `packages/plugins/examples/*`, `server`, `ui`, `cli` 포함, exclude 2 개 (`!packages/plugins/sandbox-providers/**`, `!packages/plugins/examples/plugin-orchestration-smoke-example`).
- 결정: 두 항목 누락 정정.

### 2. L87 어댑터 목록 — cursor-cloud 누락
- 코드 (R05/R06 합의): 9 개 = acpx-local · claude-local · codex-local · cursor-cloud · cursor-local · gemini-local · openclaw-gateway · opencode-local · pi-local.
- 결정: 알파벳순 9 개 완전 목록.

### 3. L87 패키지명 `@paperclip/server` → `@paperclipai/server`
- 코드 (`server/package.json`): name = `@paperclipai/server`.
- 결정: 정정.

### 4. L54 overrides 위치
- 코드 (`package.json` 루트): `"pnpm": { "overrides": { ... } }`, packageManager = pnpm@9.15.4.
- 결정: yaml/json 두 위치 통용 명시 + Paperclip 은 json 사용 표기.
