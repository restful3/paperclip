# ch10 Round 1 Codex Review

## 정합성

### F1

- **위치**: line 32
- **현재 본문 인용**: `| R6 | [Drizzle + PGlite](../research/06-drizzle-pglite.md) | 타입-안전 ORM · 임베디드 Postgres | packages/db/* (80+ 테이블 · 마이그레이션) |`
- **문제 카테고리**: 정합성
- **제안 수정안**: `packages/db/* (pgTable 88개 · company_id 보유 67개 · schema 파일 79개 · 마이그레이션)` 처럼 그룹 B 표준 숫자로 맞춘다.
- **근거**: 그룹 B handout 표준은 DB `pgTable` 88개, `company_id` 보유 67개, schema 파일 79개로 합의되어 있다(`docs/reviews/2026-05-24-grp-b-codex-handout.md`). Change-Impact도 `packages/db/src/schema/` 파일 79개와 `pgTable` 88개 증가를 명시한다(`docs/report/CHANGE-IMPACT-2026-05-23.md:57-59`).

### F2

- **위치**: line 33
- **현재 본문 인용**: `| R7 | [pnpm workspace](../research/07-pnpm-monorepo.md) | content-addressable 스토어 · `workspace:*` · filter | pnpm-workspace.yaml + 24개 워크스페이스 패키지의 분할 (adapters 9 · plugins · server · ui · cli 등) |`
- **문제 카테고리**: 정합성
- **제안 수정안**: `pnpm-workspace.yaml + 26개 워크스페이스 패키지의 분할 (packages/adapters 10개: acpx-local, claude-local, codex-local, cursor-cloud, cursor-local, gemini-local, grok-local, openclaw-gateway, opencode-local, pi-local · plugins · server · ui · cli 등; sandbox-provider 5종은 workspace에서 제외)` 로 갱신한다.
- **근거**: `pnpm-workspace.yaml:1-14` 는 `packages/*`, `packages/adapters/*`, `packages/plugins/*`, `packages/plugins/examples/*`, `server`, `ui`, `cli` 를 포함하고 `packages/plugins/sandbox-providers/**` 및 orchestration smoke example을 제외한다. 현재 매칭되는 workspace `package.json` 은 26개다. adapter workspace 패키지는 10개이며 `server/package.json:47-56` 에 10개 workspace adapter 의존성이 있다. Hermes는 workspace adapter가 아니라 외부 의존성(`server/package.json:71`)이다. built-in 등록 배열도 `grokLocalAdapter`를 포함해 13개 타입을 등록한다(`server/src/adapters/registry.ts:511-525`).

### F3

- **위치**: line 35
- **현재 본문 인용**: `| R9 | [라이브 이벤트 & heartbeat patterns](../research/09-sse-heartbeat-patterns.md) | WebSocket(메인) / SSE(plugin bridge) · liveness · watchdog | `server/src/realtime/live-events-ws.ts` + heartbeat scheduler + startup/periodic reconciliation 7-pass (`server/src/index.ts:676-780`) |`
- **문제 카테고리**: 정합성
- **제안 수정안**: `... startup reconciliation 7단계(\`server/src/index.ts:723-760\`) + periodic recovery loop(\`server/src/index.ts:789-826\`)` 로 라인 범위를 분리한다.
- **근거**: startup 7단계의 실제 호출 흐름은 `reapOrphanedRuns()`부터 `reconcileProductivityReviews()`까지 `server/src/index.ts:723-760` 에 있다. periodic loop는 `setInterval` 내부에서 같은 핵심 순서를 `server/src/index.ts:789-826` 에서 반복한다. 현재 인용 `:676-780` 은 runtime API env 설정과 timer tick 일부를 포함하면서 periodic recovery loop 본문을 빠뜨린다.

### F4

- **위치**: line 38
- **현재 본문 인용**: `표 1의 행 순서는 *외부 인프라(R6\~R9) → 오케스트레이션 패턴(R1, R10) → 실행 런타임(R2\~R5)*으로, 그림 10의 4-층 위계와 같은 순서다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: 표를 실제로 R6-R9 → R1/R10 → R2-R5 순서로 재배열하거나, 문장을 `표 1은 리서치 번호순으로 배열되어 있고, 각 행을 그림 10의 4-층 위계에 다시 대응시켜 읽을 수 있다.` 로 바꾼다.
- **근거**: 현재 표 1의 실제 행 순서는 R1, R2, R3, ..., R10 순서다(`docs/report/10-research-map.md:25-36`). 본문 설명의 순서와 표의 물리적 순서가 일치하지 않는다.

### F5

- **위치**: line 44
- **현재 본문 인용**: `Anthropic의 *"Building Effective Agents"* (Schluntz & Zhang)가 정의한 4가지 빌딩 블록이 Paperclip 어디에 대응되는지 본다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `Anthropic의 "Building Effective Agents" taxonomy 중 Paperclip과 직접 연결되는 네 도식(augmented LLM, orchestrator-workers, evaluator-optimizer, autonomous agent)을 발췌해 대응시킨다.` 로 완화한다.
- **근거**: 리서치 노트는 Anthropic 글을 workflow/agent 구분과 orchestrator-worker 개념으로 정리하고(`docs/research/01-agent-orchestration-control-plane.md:21-22`), 패턴 가이드의 핵심 패턴을 `orchestrator-worker, prompt chaining, routing, evaluator-optimizer` 등으로 적는다(`docs/research/01-agent-orchestration-control-plane.md:31`). 공식 Anthropic 글도 augmented LLM에서 시작해 compositional workflows와 autonomous agents로 복잡도를 올리는 구조이며, `orchestrator-workers`와 `evaluator-optimizer`는 그 안의 workflow 패턴이다. 따라서 "4가지 빌딩 블록"이라고 전체 taxonomy처럼 쓰면 과소 열거가 된다.

### F6

- **위치**: line 52
- **현재 본문 인용**: `그림 10-2 (Orchestrator-workers) 는 한 LLM 이 작업을 분해해 다른 LLM 워커들에 분배·종합하는 패턴이다. **이 블록이 곧 Paperclip의 "회사" 모델** 로, CEO가 분해하고, 보고 라인이 워커가 되며, control plane이 분배·종합 인프라가 된다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: `... 이 블록은 Paperclip의 "회사" 모델과 가장 가까운 외부 패턴이다. 운영 원칙상 CEO/PM 중심 분해로 쓸 수 있고, \`reports_to\` 보고 라인은 책임 추적 표면이 되며, control plane은 분배·감사·비용 귀속 인프라를 제공한다.` 로 고친다.
- **근거**: 현재 구현은 첫 회사/골/이슈를 보드 onboarding 흐름에서 만들고(`ui/src/components/OnboardingWizard.tsx:392-407`, `:552-563`), 신규 에이전트 생성은 `hire_agent` approval 흐름을 탄다(`server/src/routes/agents.ts:2028-2049`). 조직도 자체는 `agents.reports_to` 로 표현된다(`packages/db/src/schema/agents.ts:20-24`), 그러나 CEO가 자동으로 전략을 분해하는 구현은 ch08 합의처럼 SPEC 목표로 남아 있다. 따라서 "곧"은 구현 정합성상 과하다.

### F7

- **위치**: line 123
- **현재 본문 인용**: `**어댑터 계약의 단순함** — 장기 SPEC 의 `invoke/status/cancel` 3-method 계약(`doc/SPEC.md:207-215`) + 현재 구현의 `ServerAdapterModule.execute` / `testEnvironment` 2 필수 메서드(`packages/adapter-utils/src/types.ts:349-431`) + adapter package 의 4 진입점(`.` / `./server` / `./ui` / `./cli`, `packages/adapters/claude-local/package.json:15-20`) 조합으로 새 런타임을 첨가하는 비용을 작게 유지한다.`
- **문제 카테고리**: 정합성
- **제안 수정안**: 필수 메서드 인용 범위를 `packages/adapter-utils/src/types.ts:349-352` 로 좁히고, 옵션 필드까지 말하려면 `나머지는 옵션(\`listSkills\` ... \`getRuntimeCommandSpec\`, \`:353-431\`)` 처럼 분리한다.
- **근거**: `ServerAdapterModule` 의 필수 메서드는 `execute` 와 `testEnvironment` 두 줄이다(`packages/adapter-utils/src/types.ts:349-352`). `:353-431` 은 `listSkills`, `sessionCodec`, `getQuotaWindows`, `detectModel`, `getConfigSchema`, capability flags, `getRuntimeCommandSpec` 같은 optional 필드까지 포함한다.

### F8

- **위치**: lines 135-141
- **현재 본문 인용**: `# 2) /companies 에서 회사 만들고`
- **문제 카테고리**: 정합성
- **제안 수정안**: 검증 시나리오 주석을 현재 UI 흐름에 맞춰 다음처럼 바꾼다.

```bash
# 2) /onboarding 에서 회사(+선택 골)와 첫 CEO를 만들고
#    회사가 이미 있으면 /<prefix>/companies 의 New Company 또는 /<prefix>/agents/new 사용
# 3) /<prefix>/goals 또는 /<prefix>/issues 에서 작은 실행 이슈 1개 등록
# 4) /<prefix>/dashboard/live 에서 live-runs 화면 확인
# 5) /<prefix>/approvals · /api/companies/<id>/costs/summary · /<prefix>/activity 확인
# 6) hard limit을 낮게 잡아 auto-pause 트리거 확인
```

- **근거**: no-company 상태의 전역 라우트는 `/onboarding` 으로 리다이렉트한다(`ui/src/App.tsx:200-218`, `:224-240`; `ui/src/lib/onboarding-route.ts:46-50`). `/companies` 자체는 회사가 있을 때 prefixed board route로 보내지고(`ui/src/App.tsx:302`, `:331-333`), 회사 목록 화면의 `New Company` 버튼이 onboarding dialog를 연다(`ui/src/pages/Companies.tsx:90-96`). onboarding은 회사/선택 goal을 만들고(`ui/src/components/OnboardingWizard.tsx:392-407`), step 2에서 CEO hire를 만든다(`ui/src/components/OnboardingWizard.tsx:443-449`). `/dashboard/live` 는 route로 존재한다(`ui/src/App.tsx:69-70`) and live-runs 패널을 렌더한다(`ui/src/pages/DashboardLive.tsx:50-61`).

## 표현

이상 없음.

## 품질

### Q1

- **위치**: line 17
- **현재 본문 인용**: `그림 10은 두 방향으로 해석할 수 있다. (1) *밖에서 안으로* 보면 Paperclip의 모든 기능은 결국 *바깥 4개 층*에 정의된 표준/패턴/런타임의 조합이다.`
- **문제 카테고리**: 품질
- **제안 수정안**: `... 결국 *바깥 층들*에 정의된 표준/패턴/런타임과 Paperclip 코어의 조합이다.` 또는 `... 결국 앞의 네 층에 배치된 표준/패턴/런타임/코어의 조합이다.`
- **근거**: line 11은 전체를 4개 동심층으로 정의하며 그중 하나가 `Paperclip 코어`다. 따라서 `바깥 4개 층`은 "4개가 모두 바깥층"처럼 읽혀 그림 10의 계층 설명과 어긋난다.
