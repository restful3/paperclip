# R08 라운드 1 결정

- ACCEPT 2 / REJECT 0 / DEFER 0

### 1. L43 TanStack Query 훅 위치
- 코드: `ui/src/api/` 는 fetch 래퍼 위주 (`client.ts:15-50`). TanStack Query 훅은 `context/` (CompanyContext, LiveUpdatesProvider, EditorAutocompleteContext) 와 pages/components 에 분산.
- 결정: 책임 분리 표기.

### 2. L43 CSRF 동작 주장
- 코드 검색: `ui/src/`, `server/src/` 모두 `csrf|xsrf` 매치 0 건. Better Auth 쿠키·trusted origin (`server/src/auth/better-auth.ts:40-44`, `:64-90`) 만 존재.
- 결정: "쿠키·CSRF 가 자연스럽게 동작" 표현 제거, Better Auth 설정을 명시.
