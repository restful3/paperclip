# ch08 Round 5 리뷰 — 08-usage.md

## 정합성

### F1

- **위치**: line 192
- **현재 본문 인용**: `` `local_trusted` 로 오래 사용하다 `authenticated` 로 전환하는 경우 — instance admin 이 `local-board` 한 명뿐일 때에 한해 — 부팅 시 1회용 board-claim URL 이 경고로 표시되어, 로그인한 사용자가 instance admin 으로 승격되고 `local-board` 권한이 강등된다(`doc/DEPLOYMENT-MODES.md:115-126`, `server/src/board-claim.ts:44-73`, `server/src/routes/access.ts:2454-2495`). ``
- **문제 카테고리**: 정합성
- **제안 수정안**: `server/src/routes/access.ts:2454-2495` → `server/src/routes/access.ts:2408-2453`
- **근거**: board-claim 라우트는 현재 `GET /board-claim/:token` 과 `POST /board-claim/:token/claim` 으로 `server/src/routes/access.ts:2408-2453` 에 있다. `server/src/routes/access.ts:2456-2495` 는 `cli-auth/challenges` 라우트 구간이라 board-claim 근거가 아니다. `server/src/board-claim.ts:44-73` 의 challenge 생성/URL 생성 인용은 유효하고, `doc/DEPLOYMENT-MODES.md:115-126` 도 claim flow 설명과 일치한다.

## 표현

이상 없음.

## 품질

이상 없음.
