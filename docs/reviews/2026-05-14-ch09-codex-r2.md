# 챕터 09 리뷰 — 라운드 2

[9-10] §3 표 2 후속 문단에 라운드 1의 "단일 보드" 과소서술이 1곳 남아 있습니다.

- 본문 인용: `*V1 의도적 범위 밖 항목* (자동 재할당 부재, 단일 보드, KB 부재, 매출 회계 부재, OAuth/SSO, 클라우드 마켓플레이스)`
- 진실 소스: 회사 멤버십은 user principal과 role을 이미 모델링합니다(`packages/db/src/schema/company_memberships.ts:4-13`). 휴먼 role도 `owner/admin/operator/viewer` 4단계입니다(`packages/shared/src/constants.ts:624-630`). V1 밖인 것은 "단일 보드" 자체가 아니라 advanced governance / multi-member boards 쪽입니다(`doc/SPEC.md:489-493`).
- 분류: ❌ 사실 오류
- 권고: 괄호 안의 `단일 보드` 를 `고급 거버넌스` 또는 `4-eye/다중 승인 거버넌스` 로 맞춰 주세요.
