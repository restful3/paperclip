핵심 구조 설명(OpenClaw를 실행 런타임, Paperclip을 상위 control plane, agentcompanies/v1을 포터블 회사 포맷으로 두는 구도)은 대체로 코드와 외부 문서에 맞습니다. 다만 출처가 가리키는 원문과 현재 CLI/어댑터 목록 사이에 substantive한 불일치가 3곳 있습니다.

### 1. OpenClaw Molty/Peter 출처 보강

**위치**: L11

**현재**: `마스코트는 우주 바닷가재 "Molty" 이며, 외침은 "EXFOLIATE! EXFOLIATE!" 다. 메인테이너는 **Peter Steinberger 와 커뮤니티**로 표기되어 있고 ([openclaw/openclaw](https://github.com/openclaw/openclaw))`

**문제**: [ACCEPT] 인용 완결성. GitHub README는 `EXFOLIATE! EXFOLIATE!`, 개인 AI assistant 설명, Node 24/22.16+ 런타임은 확인해 주지만(`https://github.com/openclaw/openclaw`: README L10-L12, L21-L28, L97-L112), `Molty`와 `Peter Steinberger & community` 표기는 현재 `openclaw.ai` 쪽에서 확인됩니다(`https://openclaw.ai/`: L171-L173). 지금 문장은 GitHub README 하나가 전체를 뒷받침하는 것처럼 읽힙니다.

**제안**: L11의 출처를 둘로 나누어 `EXFOLIATE`/런타임은 GitHub README, `Molty`/Peter/community는 `https://openclaw.ai/` 또는 공식 credits 문서로 인용하세요. §5 출처 목록에도 `openclaw.ai`를 추가하는 편이 안전합니다.

### 2. 어댑터 디렉터리 예시에서 cursor-cloud 누락

**위치**: L45

**현재**: ``어댑터 디렉터리에 `acpx-local`, `codex-local`, `cursor-local`, `claude-local`, `gemini-local`, `opencode-local`, `pi-local`, `openclaw-gateway` 가 함께 들어있는 것으로 확인됨``

**문제**: [ACCEPT] 사실 정확성/리포트 일관성. 현재 패키지형 어댑터 디렉터리에는 `cursor-cloud`도 존재하고, registry에도 `cursorCloudAdapter`가 built-in 등록 배열에 포함됩니다(`server/src/adapters/registry.ts:318-330`, `server/src/adapters/registry.ts:479-493`). Ch04 합의도 패키지형 어댑터 9종에 `cursor-cloud`를 포함하는 쪽입니다.

**제안**: 예시 의도라면 "`cursor-cloud` 등"으로 바꾸거나, 완전 목록으로 쓰려면 `cursor-cloud`를 추가하세요.

### 3. 회사 import 명령 표기가 현재 CLI와 어긋남

**위치**: L38

**현재**: `별도 카탈로그 ... 는 ... \`npx paperclipai company import\` 로 가져온다`

**문제**: [ACCEPT] 사실 정확성. `paperclipai/companies` README는 예시로 `npx paperclipai company import --from ./trail-of-bits-security`를 싣고 있지만(`https://github.com/paperclipai/companies`: L301-L312), 현재 Paperclip CLI 구현은 `company import <fromPathOrUrl>` positional 인자를 요구하고 `--from` 옵션은 없습니다(`cli/src/commands/client/company.ts:1268-1271`). 본문처럼 bare command만 쓰면 실제 실행 가능한 형태가 아닙니다.

**제안**: 현재 코드 기준으로 `npx paperclipai company import <path-or-github-url>`처럼 source 인자를 포함해 쓰세요. 외부 catalog README 예시를 언급하려면 "catalog README의 `--from` 예시는 현재 CLI 구현과 다르다"는 식으로 분리하는 것이 좋습니다.

라운드 1 요약 — ACCEPT 3건 / REJECT 0건 / DEFER 0건.
