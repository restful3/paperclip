# 챕터 03 — 라운드 1 변경 요약

## 적용 (9 ACCEPT)

| # | 위치 | 변경 |
|---|---|---|
| A1 | §3 (`:29-43`) | 어댑터 계약을 *장기 SPEC 3-메서드* + *현재 구현 ServerAdapterModule 2-필수* 두 층으로 재작성. 코드 1을 두 블록 다이어그램으로 |
| A2 | `:11, 17` | heartbeat 흐름에서 checkout 주체를 server-side로 정정. t₁ "어댑터가 atomic checkout" → "Paperclip server-side로 atomic checkout"(`issues.ts:3593-3736`). t₂에 `adapter.execute(ctx)` 명시 |
| A3 | `:17` | t₆ "락 해제" → "실행 락(`execution_run_id`) 해제, 오너십 락(`checkout_run_id`)은 별도 회복 규칙" |
| A4 | §4 (`:55-62`) | 상태 전이 정정 — `*` → blocked는 허용 진입 경로 명시, `blocked` 해제는 wake + checkout 성공 2단계. terminal 명시 |
| A5 | `:62, 84` | "explicit recovery issue" → "explicit recovery action" (전반) |
| A6 | §6 (`:77-85`) | stranded recovery 2단계 명시 — 1단계 wake retry, 2단계 `blocked` + `issue_recovery_actions` upsert. `kind/status/fingerprint/owner_*/wake_policy/monitor_policy` 컬럼 인용 |
| A7 | §7 (`:86-92`) | silent watchdog → recovery action으로 표면화 + `stale_active_run_evaluation` 이슈 형태 명시 |
| A8 | §8 (`:94-107`) | 4-pass → 7-pass startup reconciliation (실제 순서: reap → promote scheduled → resume queued → stranded → liveness → silent → productivity reviews) |
| A9 | `:113-115` | "`status()`" stale SPEC 용어 → 현재 구현 표면(heartbeat_runs.status, process metadata, output event, watchdog evidence) |

## DEFER 2 / OUT-OF-SCOPE 1 — 적용 안 함

- 그림 5-A 번호 통합은 final-summary에서 글로벌 정합화 (Ch00·Ch01·Ch02 본문도 같이 손볼 것)

## 변경 정량

- Ch03 LOC: 118 → \~122 (실질 수정 \~45줄, §3·§4·§6·§7·§8 단락 재작성)
- 전체 누적: ~145 / 200 checkpoint (Ch04 시작 시 임박)

## 사용자 보고 트리거

스킬에 따른 mandatory checkpoint:
- Round 3 시작 직전 (현재 Ch03 r1 종료 시점)
- 누적 LOC ≈ 145 (200 임박)

이번 Ch03 합의가 끝나면 final-summary 전 (Ch04 시작 전) 사용자에게 진행 상황 보고 필요.

## 다음
라운드 2 재리뷰.
