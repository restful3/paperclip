# R09 라운드 1 변경 요약

대상: `docs/research/09-sse-heartbeat-patterns.md`. ACCEPT 5/5 반영.

### 1. L13 SSE 재연결 — 지수 백오프 표현 약화
- AS-IS: "user agent 가 지수 백오프로 재접속을 시도"
- TO-BE: "내장 재연결 절차 수행 + 스펙이 지수 백오프 지연을 허용하지만 표준 의무는 아니다"

### 2. L41 Anthropic 인용 교체
- AS-IS: "operate autonomously over multiple turns"
- TO-BE: "potentially operate for many turns" + "Once the task is clear, agents plan and operate independently"

### 3. L47 어댑터 호출 정정
- AS-IS: "adapter `invoke()` 호출"
- TO-BE: "scheduler 가 wakeup enqueue → run claim → `adapter.execute(ctx)` 호출" + `adapter.invoke` 는 이벤트 타입명 분리 + `heartbeat.ts:7636`, `:7662` 인용

### 4. L47 silent-run watchdog
- AS-IS: "heartbeat 신호 없이 멈춘 run"
- TO-BE: "`lastOutputAt` 기준 출력 silence 가 suspicion threshold 를 넘긴 active run" + `recovery/service.ts:676-760` 인용

### 5. L47 execution-semantics 경로
- AS-IS: `docs/execution-semantics.md`
- TO-BE: `doc/execution-semantics.md`

## 변경된 파일
- `docs/research/09-sse-heartbeat-patterns.md` (4 라인 수정 — §5 paragraph 의 3 substring + L13 1 substring)
