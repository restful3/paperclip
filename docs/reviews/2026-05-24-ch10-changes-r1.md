# ch10 Round 1 — Changes Applied

_2026-05-24, codex-b / Round 1 → 2 송부용_

## docs/report/10-research-map.md — 9 finding + 1 동기화 수정

```diff
# Q1 (line 17)
- *바깥 4개 층*에 정의된 표준/패턴/런타임의 조합
+ *앞의 네 층에 배치된 표준/패턴/런타임/코어의 조합*

# F1 (line 32, R6 행)
- packages/db/* (80+ 테이블 · 마이그레이션)
+ packages/db/* (pgTable 88개 · company_id 보유 67개 · schema 파일 79개 · 마이그레이션)

# F2 (line 33, R7 행)
- pnpm-workspace.yaml + 24개 워크스페이스 패키지의 분할 (adapters 9 · plugins · server · ui · cli 등)
+ pnpm-workspace.yaml + 26개 워크스페이스 패키지의 분할 (packages/adapters 10개:
+ acpx-local, claude-local, codex-local, cursor-cloud, cursor-local, gemini-local,
+ grok-local, openclaw-gateway, opencode-local, pi-local · plugins · server · ui · cli 등;
+ sandbox-providers 5종은 workspace 제외)

# F3 (line 35, R9 행)
- startup/periodic reconciliation 7-pass (server/src/index.ts:676-780)
+ startup reconciliation 7단계(server/src/index.ts:723-760) + periodic recovery loop(server/src/index.ts:789-826)

# F4 (line 38, 표 1 다음 단락)
- 표 1의 행 순서는 *외부 인프라(R6\~R9) → 오케스트레이션 패턴(R1, R10) → 실행 런타임(R2\~R5)*으로, 그림 10의 4-층 위계와 같은 순서다.
+ 표 1은 리서치 번호순으로 배열되어 있고, 각 행은 그림 10의 4-층 위계(외부 인프라 R6\~R9 · 오케스트레이션 패턴 R1·R10 · Paperclip 코어 · 실행 런타임 R2\~R5)에 다시 대응시켜 읽을 수 있다.

# F5 (line 44)
- Anthropic의 "Building Effective Agents" (Schluntz & Zhang)가 정의한 4가지 빌딩 블록이 Paperclip 어디에 대응되는지 본다.
+ Anthropic의 "Building Effective Agents" (Schluntz & Zhang) taxonomy 중 Paperclip과 직접 연결되는 네 도식 — augmented LLM · orchestrator-workers · evaluator-optimizer · autonomous agent — 을 발췌해 대응시킨다 (taxonomy 전체에는 prompt chaining · routing · parallelization 등 다른 workflow 패턴도 포함된다).

# F6 (line 52)
- **이 블록이 곧 Paperclip의 "회사" 모델** 로, CEO가 분해하고, 보고 라인이 워커가 되며, control plane이 분배·종합 인프라가 된다.
+ **이 블록은 Paperclip의 "회사" 모델과 가장 가까운 외부 패턴**으로, 운영 원칙상 CEO/PM 중심 분해로 쓸 수 있고, reports_to 보고 라인은 책임 추적 표면이 되며, control plane은 분배·감사·비용 귀속 인프라를 제공한다 (CEO 의 자동 전략 분해는 ch08 합의처럼 SPEC 목표로 남아 있고, 현재 구현은 hire_agent approval 흐름 + onboarding wizard 로 회사·CEO·골을 만든다).

# F7 (line 123, 어댑터 계약)
- `ServerAdapterModule.execute / testEnvironment` 2 필수 메서드 (packages/adapter-utils/src/types.ts:349-431)
+ `ServerAdapterModule.execute / testEnvironment` 2 필수 메서드 (packages/adapter-utils/src/types.ts:349-352; 나머지는 옵션 — listSkills · sessionCodec · getQuotaWindows · detectModel · getConfigSchema · capability flags · getRuntimeCommandSpec, :353-431)

# F8 (lines 131-142, 검증 시나리오 코드 블록)
- # 2) /companies 에서 회사 만들고
- # 3) /agents/new 에서 CEO(claude_local) 1명만 만들고
- # 4) /goals 에서 사소한 골 1개 등록하고
- # 5) /dashboard/live 에서 한 사이클을 실제로 실행
- # 6) 동시에 /approvals · /api/companies/<id>/costs/summary · activity_log 확인
- # 7) hard limit을 낮게 잡아 auto-pause 트리거 확인
+ # 2) /onboarding 에서 회사(+선택 골)와 첫 CEO 를 만들고
+ #    (회사가 이미 있으면 /<prefix>/companies 의 New Company 또는 /<prefix>/agents/new 사용)
+ # 3) /<prefix>/goals 또는 /<prefix>/issues 에서 작은 실행 이슈 1개 등록
+ # 4) /<prefix>/dashboard/live 에서 live-runs 화면 확인
+ # 5) /<prefix>/approvals · /api/companies/<id>/costs/summary · /<prefix>/activity 확인
+ # 6) hard limit 을 낮게 잡아 auto-pause 트리거 확인

# 동기화 (F8 후속 — 7단계 → 6단계)
- 코드 1은 그 *7단계 한 사이클*이다 ... **코드 1. 검증 시나리오 — 7단계 한 사이클**
+ 코드 1은 그 *6단계 한 사이클*이다 ... **코드 1. 검증 시나리오 — 6단계 한 사이클**
```

## 검증 결과
- `git diff --stat docs/report/10-research-map.md`: +16 -17 (net -1 line, ~33 touched)
- F3 라인 검증: `server/src/index.ts:726` startup `reapOrphanedRuns`, `:792` periodic `reapOrphanedRuns` — Codex 인용 `:723-760` / `:789-826` 정확

## REJECT / DEFER
없음. 모든 finding ACCEPT.

## 누적 LOC delta (그룹 B)
- ch06: ~30 / ch07: ~24 / ch08: ~32 / ch09: ~15
- ch10 R1: ~16
- **그룹 B 누적: ~117 LOC**
