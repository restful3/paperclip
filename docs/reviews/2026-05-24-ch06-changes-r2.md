# ch06 Round 2 — Changes Applied

_2026-05-24, codex-b / Round 2 → 3 송부용_

## docs/report/06-ui-and-board.md — 5곳 수정

```diff
- ## 2. 라우팅 표 (Route 129개)
+ ## 2. 라우팅 표 (Route 128개)

- 들어온 `LiveEvent` 의 `type`/`entity_type` 으로 query key 를 invalidate
+ 들어온 `LiveEvent` 의 `type`/`payload.entityType` 으로 query key 를 invalidate

- ├── api/                 # fetch 래퍼 + zod 검증
+ ├── api/                 # fetch 래퍼 + 일부 응답 zod 검증

- **HMR 도 동일 origin** — Vite WS 가 그대로 동작한다.
+ **HMR** — Express 에 마운트된 Vite dev server 가 별도 HMR 포트(`server/src/app.ts:381-392`, 기본 serverPort+10000)로 WS 를 띄운다.

- // …나머지 LIVE_EVENT_TYPES (heartbeat.run.queued/status, agent.status, activity.logged) 로 분기
+ // 실제 구현은 heartbeat.run.queued/status, agent.status, activity.logged 등을 별도 handler 로 처리
```

## 누적 LOC delta (그룹 B)

- Round 1: ~22 LOC
- Round 2: ~6 LOC
- **누적: ~28 LOC** (LOC_CHECKPOINT 200 까지 172 LOC 여유)
