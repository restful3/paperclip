# R08 라운드 1 변경 요약

대상: `docs/research/08-react-vite-shadcn.md`. ACCEPT 2/2 반영.

### 1. L43 TanStack Query 훅 위치 정정
- AS-IS: "`ui/src/api/` 에 TanStack Query 훅과 서버 상태 페치 레이어, `ui/src/hooks/`·`ui/src/context/` 에 클라이언트 상태"
- TO-BE: "`ui/src/api/` 에 `/api` fetch 래퍼 (`client.ts:22-25` 의 `credentials: "include"`), TanStack Query 훅은 `context/` (CompanyContext, LiveUpdatesProvider) + pages/components 에 분산, `hooks/` 는 보조 클라이언트 훅"

### 2. L43 CSRF 주장 제거 + Better Auth 인용
- AS-IS: "외부 환경 변수 없이 쿠키·CSRF 가 자연스럽게 동작한다"
- TO-BE: "CORS 설정 없이 쿠키 기반 세션 요청이 단순해진다 — 별도 CSRF 토큰 방어는 코드 베이스에 없고 Better Auth 쿠키·trusted origin 설정 (`server/src/auth/better-auth.ts:40-44`, `:64-90`) 이 그 자리"

## 변경된 파일
- `docs/research/08-react-vite-shadcn.md` (2 라인 수정)
